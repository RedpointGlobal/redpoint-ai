{
  "name": "@redpoint-ai/mcp-rpi",
  "version": "0.1.0",
  "type": "module",
  "license": "Apache-2.0",
  "author": "Redpoint",
  "description": "MCP server for Redpoint Interaction (RPI) Integration API",
  "repository": {
    "type": "git",
    "url": "https://github.com/RedpointGlobal/redpoint-ai.git",
    "directory": "packages/mcp-rpi"
  },
  "publishConfig": {
    "access": "public"
  },
  "bin": {
    "mcp-rpi": "./src/index.ts"
  },
  "main": "./src/server.ts",
  "exports": {
    ".": "./src/server.ts"
  },
  "scripts": {
    "dev": "bun --hot run src/http.ts",
    "start": "bun run src/index.ts",
    "start:http": "bun run src/http.ts",
    "test": "bun test",
    "test:integration": "bun test src/__tests__/integration",
    "example:agent-auth": "bun run examples/agent-auth-flow.ts",
    "generate:types": "bun run scripts/generate-types.ts",
    "generate:types:check": "bun run scripts/generate-types.ts --check",
    "generate:tools": "bun run scripts/generate-tools.ts",
    "generate:tools:check": "bun run scripts/generate-tools.ts --check",
    "build": "bun run build:linux",
    "build:linux": "bun build --compile --target=bun-linux-x64 src/http.ts --outfile dist/rp-rpi-mcp-linux-x64/rp-rpi-mcp-linux",
    "build:windows": "bun build --compile --target=bun-windows-x64 src/http.ts --outfile dist/rp-rpi-mcp-windows-x64/rp-rpi-mcp-windows.exe",
    "//disabled:build:macos": "DISABLED — not in the release set (build:all = linux + windows only). Rename key back to build:macos to re-enable. bun build --compile --target=bun-darwin-arm64 src/http.ts --outfile dist/rp-rpi-mcp-macos-arm64/rp-rpi-mcp-macos-arm64",
    "//disabled:build:macos-intel": "DISABLED — not in the release set. Rename key back to build:macos-intel to re-enable. bun build --compile --target=bun-darwin-x64 src/http.ts --outfile dist/rp-rpi-mcp-macos-intel/rp-rpi-mcp-macos-intel",
    "build:all": "bun run ../../scripts/env-scopes.ts check && bun run build:linux && bun run build:windows && bun run build:copy-templates && SMOKE_SVC=rpi bun run ../../scripts/smoke-binary.ts",
    "build:copy-templates": "bun run scripts/copy-dist-templates.ts",
    "release:zip": "bun run scripts/release-zip.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.29.0",
    "hono": "4.12.25",
    "jose": "6.2.2",
    "zod": "3.25.76"
  },
  "devDependencies": {
    "@types/bun": "1.3.12",
    "adm-zip": "0.5.16",
    "openapi-typescript": "7.13.0"
  }
}
