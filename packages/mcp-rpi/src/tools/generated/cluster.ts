// AUTO-GENERATED by scripts/generate-tools.ts — DO NOT EDIT.
// In-scope RPI GET endpoints (#27634). Regenerate: bun run generate:tools
// All tools are read-only (GET); descriptions come from src/tools/overlay.ts.

import { z } from "zod";
import type { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js";
import type { RPIApiClient } from "../../client/rpi-api.js";
import { createToolRegistrar } from "../../tool-categories.js";
import { clientIdSchema, verboseSchema, jsonContent, errorContent, READ_ONLY_ANNOTATIONS, targetUrlOf } from "../generated-shared.js";

export function registerClusterGeneratedTools(server: McpServer, rpiClient: RPIApiClient): void {
  const registerTool = createToolRegistrar(server, "cluster");
  void rpiClient;
  registerTool(
    "get_cluster_client_auxiliary_databases",
    {
      _meta: { endpoints: ["/cluster/operations/client/auxiliary-databases"] },
      title: "Get Cluster Client Auxiliary Databases",
      description: "List the auxiliary databases configured for a cluster client/tenant (ID = the client) — the client's secondary data sources beyond its primary database. Read-only.",
      inputSchema: {
      "ID": z.string().optional().describe("ID (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ ID, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/cluster/operations/client/auxiliary-databases";
        const query: Record<string, string> = {};
      if (ID !== undefined) query["ID"] = String(ID);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_cluster_client_auxiliary_databases", error);
      }
    },
  );
  registerTool(
    "get_cluster_external_user_clients",
    {
      _meta: { endpoints: ["/cluster/configuration/external-user/clients"] },
      title: "Get Cluster External User Clients",
      description: "List the RPI clients an EXTERNAL (federated) cluster user (ID) can access. The internal-user equivalent is get_cluster_user_clients. Read-only.",
      inputSchema: {
      "ID": z.string().optional().describe("ID (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ ID, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/cluster/configuration/external-user/clients";
        const query: Record<string, string> = {};
      if (ID !== undefined) query["ID"] = String(ID);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_cluster_external_user_clients", error);
      }
    },
  );
  registerTool(
    "get_cluster_external_user",
    {
      _meta: { endpoints: ["/cluster/configuration/external-user"] },
      title: "Get Cluster External User",
      description: "Get one EXTERNAL (federated/SSO) cluster user by ID. The internal-account equivalent is get_cluster_user; this external user's accessible clients: get_cluster_external_user_clients. Read-only.",
      inputSchema: {
      "ID": z.string().optional().describe("ID (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ ID, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/cluster/configuration/external-user";
        const query: Record<string, string> = {};
      if (ID !== undefined) query["ID"] = String(ID);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_cluster_external_user", error);
      }
    },
  );
  registerTool(
    "list_cluster_external_users",
    {
      _meta: { endpoints: ["/cluster/configuration/external-users"] },
      title: "List Cluster External Users",
      description: "List all EXTERNAL cluster users — federated/SSO identities rather than internal RPI accounts (list_cluster_users) — each with id and name. Fetch one by id: get_cluster_external_user. Read-only.",
      inputSchema: {
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/cluster/configuration/external-users";
        const query: Record<string, string> = {};
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in list_cluster_external_users", error);
      }
    },
  );
  registerTool(
    "get_cluster_error_log",
    {
      _meta: { endpoints: ["/cluster/operations/logs/error"] },
      title: "Get Cluster Error Log",
      description: "Get the cluster's GENERAL error log (paged via PageNumber/PageSize) — runtime errors across the cluster. For API-specific errors use get_cluster_api_error_log; for maintenance-job errors use get_cluster_housekeeping_log. Read-only.",
      inputSchema: {
      "PageNumber": z.string().optional().describe("PageNumber (query parameter)"),
      "PageSize": z.string().optional().describe("PageSize (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ PageNumber, PageSize, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/cluster/operations/logs/error";
        const query: Record<string, string> = {};
      if (PageNumber !== undefined) query["PageNumber"] = String(PageNumber);
      if (PageSize !== undefined) query["PageSize"] = String(PageSize);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_cluster_error_log", error);
      }
    },
  );
  registerTool(
    "get_cluster_housekeeping_log",
    {
      _meta: { endpoints: ["/cluster/operations/logs/house-keeping-log"] },
      title: "Get Cluster Housekeeping Log",
      description: "Get the cluster's HOUSEKEEPING log — the record of maintenance/cleanup jobs (paged). Distinct from the general error log (get_cluster_error_log) and the API error log (get_cluster_api_error_log). Read-only.",
      inputSchema: {
      "PageNumber": z.string().optional().describe("PageNumber (query parameter)"),
      "PageSize": z.string().optional().describe("PageSize (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ PageNumber, PageSize, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/cluster/operations/logs/house-keeping-log";
        const query: Record<string, string> = {};
      if (PageNumber !== undefined) query["PageNumber"] = String(PageNumber);
      if (PageSize !== undefined) query["PageSize"] = String(PageSize);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_cluster_housekeeping_log", error);
      }
    },
  );
  registerTool(
    "get_cluster_system_tasks",
    {
      _meta: { endpoints: ["/cluster/operations/system-tasks"] },
      title: "Get Cluster System Tasks",
      description: "List the cluster's SYSTEM TASKS — background/scheduled cluster-wide jobs and their state (paged). Cluster scope; the client-scoped system tasks are a separate operations-domain read. Read-only.",
      inputSchema: {
      "PageNumber": z.string().optional().describe("PageNumber (query parameter)"),
      "PageSize": z.string().optional().describe("PageSize (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ PageNumber, PageSize, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/cluster/operations/system-tasks";
        const query: Record<string, string> = {};
      if (PageNumber !== undefined) query["PageNumber"] = String(PageNumber);
      if (PageSize !== undefined) query["PageSize"] = String(PageSize);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_cluster_system_tasks", error);
      }
    },
  );
  registerTool(
    "list_cluster_plugins",
    {
      _meta: { endpoints: ["/cluster/configuration/plugins"] },
      title: "List Cluster Plugins",
      description: "List the plugins/extensions installed on the RPI cluster, each with id and name. Read-only.",
      inputSchema: {
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/cluster/configuration/plugins";
        const query: Record<string, string> = {};
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in list_cluster_plugins", error);
      }
    },
  );
  registerTool(
    "get_cluster_user_clients",
    {
      _meta: { endpoints: ["/cluster/configuration/user/clients"] },
      title: "Get Cluster User Clients",
      description: "List the RPI clients (tenants) a given cluster-level internal user (ID) can access. The external-user equivalent is get_cluster_external_user_clients. Read-only.",
      inputSchema: {
      "ID": z.string().optional().describe("ID (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ ID, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/cluster/configuration/user/clients";
        const query: Record<string, string> = {};
      if (ID !== undefined) query["ID"] = String(ID);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_cluster_user_clients", error);
      }
    },
  );
  registerTool(
    "get_cluster_user",
    {
      _meta: { endpoints: ["/cluster/configuration/user"] },
      title: "Get Cluster User",
      description: "Get one cluster-level RPI user by ID — an internal account defined at the cluster. By name: get_cluster_user_by_name; its accessible clients: get_cluster_user_clients; its profile: get_cluster_user_profile. For a federated/external identity use get_cluster_external_user. Read-only.",
      inputSchema: {
      "ID": z.string().optional().describe("ID (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ ID, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/cluster/configuration/user";
        const query: Record<string, string> = {};
      if (ID !== undefined) query["ID"] = String(ID);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_cluster_user", error);
      }
    },
  );
  registerTool(
    "get_cluster_user_by_name",
    {
      _meta: { endpoints: ["/cluster/configuration/user/by-name"] },
      title: "Get Cluster User By Name",
      description: "Get one cluster-level RPI user by exact name — the same record as get_cluster_user, keyed by name. Read-only.",
      inputSchema: {
      "Name": z.string().optional().describe("Name (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ Name, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/cluster/configuration/user/by-name";
        const query: Record<string, string> = {};
      if (Name !== undefined) query["Name"] = String(Name);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_cluster_user_by_name", error);
      }
    },
  );
  registerTool(
    "get_cluster_user_profile",
    {
      _meta: { endpoints: ["/cluster/configuration/user/profile"] },
      title: "Get Cluster User Profile",
      description: "Get the profile (settings/roles at the cluster) of a NAMED cluster-level user by ID. This is a specific user's profile — NOT the authenticated caller's own profile, which is get_user_profile. Read-only.",
      inputSchema: {
      "ID": z.string().optional().describe("ID (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ ID, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/cluster/configuration/user/profile";
        const query: Record<string, string> = {};
      if (ID !== undefined) query["ID"] = String(ID);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_cluster_user_profile", error);
      }
    },
  );
  registerTool(
    "list_cluster_users",
    {
      _meta: { endpoints: ["/cluster/configuration/users"] },
      title: "List Cluster Users",
      description: "List all cluster-level RPI users — internal RPI-managed accounts defined at the cluster (spanning all clients), each with id and name. For federated/SSO identities use list_cluster_external_users. Fetch one by id (get_cluster_user) or name (get_cluster_user_by_name). Read-only.",
      inputSchema: {
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/cluster/configuration/users";
        const query: Record<string, string> = {};
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in list_cluster_users", error);
      }
    },
  );
}
