// AUTO-GENERATED by scripts/generate-tools.ts — DO NOT EDIT.
// In-scope RPI GET endpoints (#27634). Regenerate: bun run generate:tools
// All tools are read-only (GET); descriptions come from src/tools/overlay.ts.

import type { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js";
import type { RPIApiClient } from "../../client/rpi-api.js";
import { createToolRegistrar } from "../../tool-categories.js";
import { clientIdSchema, verboseSchema, jsonContent, errorContent, READ_ONLY_ANNOTATIONS, targetUrlOf } from "../generated-shared.js";

export function registerAuthGeneratedTools(server: McpServer, rpiClient: RPIApiClient): void {
  const registerTool = createToolRegistrar(server, "auth");
  void rpiClient;
  registerTool(
    "get_user_profile",
    {
      _meta: { endpoints: ["/authentication/user-profile"] },
      title: "Get User Profile",
      description: "Get the authenticated user's RPI profile — display name, username, email, and assigned roles/permissions for the token making the call. Use to confirm who the current credentials represent and what they're authorized to do. Read-only.",
      inputSchema: {
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/authentication/user-profile";
        const query: Record<string, string> = {};
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_user_profile", error);
      }
    },
  );
  registerTool(
    "get_user_client_list",
    {
      _meta: { endpoints: ["/authentication/user-client-list"] },
      title: "Get User Client List",
      description: "List the RPI clients (tenants) the authenticated user can access, each with its id and name. Use to discover which X-ClientID values are valid for this user before scoping audience, interaction, or selection-rule calls to a specific client. Read-only.",
      inputSchema: {
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/authentication/user-client-list";
        const query: Record<string, string> = {};
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_user_client_list", error);
      }
    },
  );
  registerTool(
    "validate_token_status",
    {
      _meta: { endpoints: ["/authentication/validate-token-status"] },
      title: "Validate Token Status",
      description: "Check whether the caller's RPI access token is currently valid (active, not expired or revoked). Use as a lightweight liveness/auth check before a longer sequence of calls. Read-only.",
      inputSchema: {
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/authentication/validate-token-status";
        const query: Record<string, string> = {};
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in validate_token_status", error);
      }
    },
  );
  registerTool(
    "get_user_recent_items",
    {
      _meta: { endpoints: ["/client/settings/user-recent-items"] },
      title: "Get User Recent Items",
      description: "Get the authenticated user's recently-accessed items — the caller's recent files/objects (user-scoped convenience list). No ID. Read-only.",
      inputSchema: {
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/settings/user-recent-items";
        const query: Record<string, string> = {};
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_user_recent_items", error);
      }
    },
  );
  registerTool(
    "get_login_settings",
    {
      _meta: { endpoints: ["/authentication/login-settings"] },
      title: "Get Login Settings",
      description: "Get the tenant's login/authentication settings — the configured identity provider (e.g. OpenID/Keycloak) and login options. Use to see how sign-in is configured. Read-only.",
      inputSchema: {
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/authentication/login-settings";
        const query: Record<string, string> = {};
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_login_settings", error);
      }
    },
  );
}
