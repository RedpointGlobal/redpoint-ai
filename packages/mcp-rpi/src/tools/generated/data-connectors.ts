// AUTO-GENERATED by scripts/generate-tools.ts — DO NOT EDIT.
// In-scope RPI GET endpoints (#27634). Regenerate: bun run generate:tools
// All tools are read-only (GET); descriptions come from src/tools/overlay.ts.

import { z } from "zod";
import type { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js";
import type { RPIApiClient } from "../../client/rpi-api.js";
import { createToolRegistrar } from "../../tool-categories.js";
import { clientIdSchema, verboseSchema, jsonContent, errorContent, READ_ONLY_ANNOTATIONS, targetUrlOf } from "../generated-shared.js";

export function registerDataConnectorsGeneratedTools(server: McpServer, rpiClient: RPIApiClient): void {
  const registerTool = createToolRegistrar(server, "data-connectors");
  void rpiClient;
  registerTool(
    "get_data_connector_sync_definition",
    {
      _meta: { endpoints: ["/client/data-connector"] },
      title: "Get Data Connector Sync Definition",
      description: "Get one data-connector sync definition by ID — the configuration of a connector sync (what data flows to which external destination). For live run status use list_data_connector_sync_info. The activate/deactivate actions are intentionally not exposed on this read-only surface. Read-only.",
      inputSchema: {
      "ID": z.string().optional().describe("ID (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ ID, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/data-connector";
        const query: Record<string, string> = {};
      if (ID !== undefined) query["ID"] = String(ID);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_data_connector_sync_definition", error);
      }
    },
  );
  registerTool(
    "list_data_connector_sync_info",
    {
      _meta: { endpoints: ["/client/data-connectors"] },
      title: "List Data Connector Sync Info",
      description: "List data-connector sync status/info for a given workflow association (WorkflowAssociationID) — the run state of connector syncs. For a sync's configuration use get_data_connector_sync_definition. Read-only.",
      inputSchema: {
      "WorkflowAssociationID": z.string().optional().describe("WorkflowAssociationID (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ WorkflowAssociationID, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/data-connectors";
        const query: Record<string, string> = {};
      if (WorkflowAssociationID !== undefined) query["WorkflowAssociationID"] = String(WorkflowAssociationID);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in list_data_connector_sync_info", error);
      }
    },
  );
}
