// AUTO-GENERATED by scripts/generate-tools.ts — DO NOT EDIT.
// In-scope RPI GET endpoints (#27634). Regenerate: bun run generate:tools
// All tools are read-only (GET); descriptions come from src/tools/overlay.ts.

import { z } from "zod";
import type { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js";
import type { RPIApiClient } from "../../client/rpi-api.js";
import { createToolRegistrar } from "../../tool-categories.js";
import { clientIdSchema, verboseSchema, jsonContent, errorContent, READ_ONLY_ANNOTATIONS, targetUrlOf } from "../generated-shared.js";

export function registerJobsGeneratedTools(server: McpServer, rpiClient: RPIApiClient): void {
  const registerTool = createToolRegistrar(server, "jobs");
  void rpiClient;
  registerTool(
    "get_job_log",
    {
      _meta: { endpoints: ["/client/jobs/job/log"] },
      title: "Get Job Log",
      description: "Get the log output of a job by its ID (paged via PageNumber) — the run messages. For the job's status/progress use get_job_status. Read-only.",
      inputSchema: {
      "ID": z.string().optional().describe("ID (query parameter)"),
      "PageNumber": z.string().optional().describe("PageNumber (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ ID, PageNumber, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/jobs/job/log";
        const query: Record<string, string> = {};
      if (ID !== undefined) query["ID"] = String(ID);
      if (PageNumber !== undefined) query["PageNumber"] = String(PageNumber);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_job_log", error);
      }
    },
  );
  registerTool(
    "get_job_status",
    {
      _meta: { endpoints: ["/client/jobs/job/status"] },
      title: "Get Job Status",
      description: "Get the status of a job by its ID — current state/progress of an async job (paged via PageNumber). For the job's log output use get_job_log. Read-only.",
      inputSchema: {
      "ID": z.string().optional().describe("ID (query parameter)"),
      "PageNumber": z.string().optional().describe("PageNumber (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ ID, PageNumber, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/jobs/job/status";
        const query: Record<string, string> = {};
      if (ID !== undefined) query["ID"] = String(ID);
      if (PageNumber !== undefined) query["PageNumber"] = String(PageNumber);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_job_status", error);
      }
    },
  );
}
