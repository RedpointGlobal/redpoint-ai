// AUTO-GENERATED by scripts/generate-tools.ts — DO NOT EDIT.
// In-scope RPI GET endpoints (#27634). Regenerate: bun run generate:tools
// All tools are read-only (GET); descriptions come from src/tools/overlay.ts.

import { z } from "zod";
import type { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js";
import type { RPIApiClient } from "../../client/rpi-api.js";
import { createToolRegistrar } from "../../tool-categories.js";
import { clientIdSchema, verboseSchema, jsonContent, errorContent, READ_ONLY_ANNOTATIONS, targetUrlOf } from "../generated-shared.js";

export function registerDataImportGeneratedTools(server: McpServer, rpiClient: RPIApiClient): void {
  const registerTool = createToolRegistrar(server, "data-import");
  void rpiClient;
  registerTool(
    "get_data_import_file",
    {
      _meta: { endpoints: ["/client/data-import-file"] },
      title: "Get Data Import File",
      description: "Get one data-import file by ID — the definition/metadata of a data import. Its cleanup history: get_data_import_file_housekeeping_records. Distinct from the data-import file SYSTEM (get_data_import_file_system_configuration / list_data_import_file_system_files). Read-only.",
      inputSchema: {
      "ID": z.string().optional().describe("ID (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ ID, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/data-import-file";
        const query: Record<string, string> = {};
      if (ID !== undefined) query["ID"] = String(ID);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_data_import_file", error);
      }
    },
  );
  registerTool(
    "get_data_import_file_housekeeping_records",
    {
      _meta: { endpoints: ["/client/data-import-file/housekeeping-records"] },
      title: "Get Data Import File Housekeeping Records",
      description: "Get the housekeeping (cleanup/retention) records for a data-import file (ID). For the import file itself use get_data_import_file. Read-only.",
      inputSchema: {
      "ID": z.string().optional().describe("ID (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ ID, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/data-import-file/housekeeping-records";
        const query: Record<string, string> = {};
      if (ID !== undefined) query["ID"] = String(ID);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_data_import_file_housekeeping_records", error);
      }
    },
  );
  registerTool(
    "get_data_import_file_system_configuration",
    {
      _meta: { endpoints: ["/client/data-import-file-system/configuration"] },
      title: "Get Data Import File System Configuration",
      description: "Get the data-import file-SYSTEM configuration — how the client's import drop-locations/paths are set up. To list the files present use list_data_import_file_system_files; for a specific import file use get_data_import_file. Read-only.",
      inputSchema: {
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/data-import-file-system/configuration";
        const query: Record<string, string> = {};
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_data_import_file_system_configuration", error);
      }
    },
  );
  registerTool(
    "list_data_import_file_system_files",
    {
      _meta: { endpoints: ["/client/data-import-file-system/files"] },
      title: "List Data Import File System Files",
      description: "List the files present in the data-import file system — optionally filtered by ExtensionFilter and recursing subdirectories (IncludeSubDirectories). The FS configuration is get_data_import_file_system_configuration. Read-only.",
      inputSchema: {
      "ExtensionFilter": z.string().optional().describe("ExtensionFilter (query parameter)"),
      "IncludeSubDirectories": z.string().optional().describe("IncludeSubDirectories (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ ExtensionFilter, IncludeSubDirectories, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/data-import-file-system/files";
        const query: Record<string, string> = {};
      if (ExtensionFilter !== undefined) query["ExtensionFilter"] = String(ExtensionFilter);
      if (IncludeSubDirectories !== undefined) query["IncludeSubDirectories"] = String(IncludeSubDirectories);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in list_data_import_file_system_files", error);
      }
    },
  );
}
