// AUTO-GENERATED by scripts/generate-tools.ts — DO NOT EDIT.
// In-scope RPI GET endpoints (#27634). Regenerate: bun run generate:tools
// All tools are read-only (GET); descriptions come from src/tools/overlay.ts.

import { z } from "zod";
import type { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js";
import type { RPIApiClient } from "../../client/rpi-api.js";
import { createToolRegistrar } from "../../tool-categories.js";
import { clientIdSchema, verboseSchema, jsonContent, errorContent, READ_ONLY_ANNOTATIONS, targetUrlOf } from "../generated-shared.js";

export function registerFoldersGeneratedTools(server: McpServer, rpiClient: RPIApiClient): void {
  const registerTool = createToolRegistrar(server, "folders");
  void rpiClient;
  registerTool(
    "get_user_private_folder",
    {
      _meta: { endpoints: ["/client/file-system/folder/user-private-folder"] },
      title: "Get User Private Folder",
      description: "Get the authenticated user's private (home) folder — the personal folder for the current caller. No ID. Read-only.",
      inputSchema: {
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/file-system/folder/user-private-folder";
        const query: Record<string, string> = {};
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_user_private_folder", error);
      }
    },
  );
  registerTool(
    "get_folder_by_full_path",
    {
      _meta: { endpoints: ["/client/file-system/folder/by-fullpath"] },
      title: "Get Folder By Full Path",
      description: "Get one folder by its full path string (e.g. '/Audiences/MW') — the folder's properties (name, parent, path). Folders are readable by full path only; there is no folder-by-ID read — resolve a file's folder from the get_file_info_by_id card, or browse the tree with list_folders. Read-only.",
      inputSchema: {
      "FullPath": z.string().optional().describe("FullPath (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ FullPath, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/file-system/folder/by-fullpath";
        const query: Record<string, string> = {};
      if (FullPath !== undefined) query["FullPath"] = String(FullPath);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_folder_by_full_path", error);
      }
    },
  );
  registerTool(
    "get_folder_permissions",
    {
      _meta: { endpoints: ["/client/file-system/folder/permissions"] },
      title: "Get Folder Permissions",
      description: "Get the access permissions on a folder by its ID — which users/groups can see/use it. By path instead: get_folder_permissions_by_full_path. Read-only.",
      inputSchema: {
      "ID": z.string().optional().describe("ID (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ ID, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/file-system/folder/permissions";
        const query: Record<string, string> = {};
      if (ID !== undefined) query["ID"] = String(ID);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_folder_permissions", error);
      }
    },
  );
  registerTool(
    "get_folder_permissions_by_full_path",
    {
      _meta: { endpoints: ["/client/file-system/folder/by-fullpath/permissions"] },
      title: "Get Folder Permissions By Full Path",
      description: "Get the access permissions on a folder by its full path — the same as get_folder_permissions, keyed by path. Read-only.",
      inputSchema: {
      "FullPath": z.string().optional().describe("FullPath (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ FullPath, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/file-system/folder/by-fullpath/permissions";
        const query: Record<string, string> = {};
      if (FullPath !== undefined) query["FullPath"] = String(FullPath);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_folder_permissions_by_full_path", error);
      }
    },
  );
  registerTool(
    "list_folder_content",
    {
      _meta: { endpoints: ["/client/file-system/folders/content"] },
      title: "List Folder Content",
      description: "List the CONTENTS of a folder (ID) — the files and subfolders directly inside it. Distinct from the list_folders hand tool (which walks the whole folder TREE); for a folder's own properties use get_folder_by_full_path. Read-only.",
      inputSchema: {
      "ID": z.string().optional().describe("ID (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ ID, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/file-system/folders/content";
        const query: Record<string, string> = {};
      if (ID !== undefined) query["ID"] = String(ID);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in list_folder_content", error);
      }
    },
  );
}
