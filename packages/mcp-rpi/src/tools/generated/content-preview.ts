// AUTO-GENERATED by scripts/generate-tools.ts — DO NOT EDIT.
// In-scope RPI GET endpoints (#27634). Regenerate: bun run generate:tools
// All tools are read-only (GET); descriptions come from src/tools/overlay.ts.

import { z } from "zod";
import type { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js";
import type { RPIApiClient } from "../../client/rpi-api.js";
import { createToolRegistrar } from "../../tool-categories.js";
import { clientIdSchema, verboseSchema, jsonContent, errorContent, READ_ONLY_ANNOTATIONS, targetUrlOf } from "../generated-shared.js";

export function registerContentPreviewGeneratedTools(server: McpServer, rpiClient: RPIApiClient): void {
  const registerTool = createToolRegistrar(server, "content-preview");
  void rpiClient;
  registerTool(
    "get_content_preview_html",
    {
      _meta: { endpoints: ["/client/content-preview/html"] },
      title: "Get Content Preview Html",
      description: "Render a content preview as HTML — the resolved content for a file/content/template (FileID + ContentID + TemplateID; optional DynamicContentInstanceHashID for a specific dynamic-content instance). For the available variants first use get_content_combinations. Read-only.",
      inputSchema: {
      "FileID": z.string().optional().describe("FileID (query parameter)"),
      "ContentID": z.string().optional().describe("ContentID (query parameter)"),
      "TemplateID": z.string().optional().describe("TemplateID (query parameter)"),
      "DynamicContentInstanceHashID": z.string().optional().describe("DynamicContentInstanceHashID (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ FileID, ContentID, TemplateID, DynamicContentInstanceHashID, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/content-preview/html";
        const query: Record<string, string> = {};
      if (FileID !== undefined) query["FileID"] = String(FileID);
      if (ContentID !== undefined) query["ContentID"] = String(ContentID);
      if (TemplateID !== undefined) query["TemplateID"] = String(TemplateID);
      if (DynamicContentInstanceHashID !== undefined) query["DynamicContentInstanceHashID"] = String(DynamicContentInstanceHashID);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_content_preview_html", error);
      }
    },
  );
  registerTool(
    "get_content_combinations",
    {
      _meta: { endpoints: ["/client/content-preview/combinations"] },
      title: "Get Content Combinations",
      description: "Get the available content combinations for a file/content/template (FileID + ContentID + TemplateID) — the variants that can be previewed. Render one as HTML with get_content_preview_html. Read-only.",
      inputSchema: {
      "FileID": z.string().optional().describe("FileID (query parameter)"),
      "ContentID": z.string().optional().describe("ContentID (query parameter)"),
      "TemplateID": z.string().optional().describe("TemplateID (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ FileID, ContentID, TemplateID, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/content-preview/combinations";
        const query: Record<string, string> = {};
      if (FileID !== undefined) query["FileID"] = String(FileID);
      if (ContentID !== undefined) query["ContentID"] = String(ContentID);
      if (TemplateID !== undefined) query["TemplateID"] = String(TemplateID);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_content_combinations", error);
      }
    },
  );
}
