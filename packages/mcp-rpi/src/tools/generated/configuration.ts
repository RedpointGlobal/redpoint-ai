// AUTO-GENERATED by scripts/generate-tools.ts — DO NOT EDIT.
// In-scope RPI GET endpoints (#27634). Regenerate: bun run generate:tools
// All tools are read-only (GET); descriptions come from src/tools/overlay.ts.

import { z } from "zod";
import type { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js";
import type { RPIApiClient } from "../../client/rpi-api.js";
import { createToolRegistrar } from "../../tool-categories.js";
import { clientIdSchema, verboseSchema, jsonContent, errorContent, READ_ONLY_ANNOTATIONS, targetUrlOf } from "../generated-shared.js";

export function registerConfigurationGeneratedTools(server: McpServer, rpiClient: RPIApiClient): void {
  const registerTool = createToolRegistrar(server, "configuration");
  void rpiClient;
  registerTool(
    "get_attribute_list",
    {
      _meta: { endpoints: ["/client/configuration/attribute-list"] },
      title: "Get Attribute List",
      description: "Get ONE attribute list by its ID, including the attributes it contains. An attribute list is a reusable named set of client-data attributes referenced by selection rules and audiences. Use when you already have the list's ID; to look it up by name use get_attribute_list_by_name, or list_attribute_lists to browse them all. Read-only.",
      inputSchema: {
      "ID": z.string().optional().describe("ID (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ ID, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/configuration/attribute-list";
        const query: Record<string, string> = {};
      if (ID !== undefined) query["ID"] = String(ID);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_attribute_list", error);
      }
    },
  );
  registerTool(
    "get_attribute_list_by_name",
    {
      _meta: { endpoints: ["/client/configuration/attribute-list/by-name"] },
      title: "Get Attribute List By Name",
      description: "Get ONE attribute list by its exact name (case-sensitive), including the attributes it contains. Same record as get_attribute_list but keyed by name instead of ID — use when the user names the list rather than its ID. list_attribute_lists browses all of them. Read-only.",
      inputSchema: {
      "Name": z.string().optional().describe("Name (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ Name, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/configuration/attribute-list/by-name";
        const query: Record<string, string> = {};
      if (Name !== undefined) query["Name"] = String(Name);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_attribute_list_by_name", error);
      }
    },
  );
  registerTool(
    "list_attribute_lists",
    {
      _meta: { endpoints: ["/client/configuration/attribute-lists"] },
      title: "List Attribute Lists",
      description: "List the attribute lists configured in the RPI tenant. Attribute lists are reusable named sets of client-data attributes referenced by selection rules, audiences, and interactions; each entry returns its id, name, and description. Read-only.",
      inputSchema: {
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/configuration/attribute-lists";
        const query: Record<string, string> = {};
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in list_attribute_lists", error);
      }
    },
  );
  registerTool(
    "get_attribute_value_list",
    {
      _meta: { endpoints: ["/client/attribute-values"] },
      title: "Get Attribute Value List",
      description: "Get the enumerated VALUES of an attribute value list by ID (pass ForceRefresh to bypass any cache) — the values themselves, not the list of lists. Distinct from get_attribute_list (a reusable set of attributes) and list_value_lists (the value-list definitions). Read-only.",
      inputSchema: {
      "ID": z.string().optional().describe("ID (query parameter)"),
      "ForceRefresh": z.string().optional().describe("ForceRefresh (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ ID, ForceRefresh, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/attribute-values";
        const query: Record<string, string> = {};
      if (ID !== undefined) query["ID"] = String(ID);
      if (ForceRefresh !== undefined) query["ForceRefresh"] = String(ForceRefresh);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_attribute_value_list", error);
      }
    },
  );
  registerTool(
    "get_audience_snapshot",
    {
      _meta: { endpoints: ["/client/configuration/audience-snapshot"] },
      title: "Get Audience Snapshot",
      description: "Get one audience snapshot by its ID — a frozen point-in-time audience membership. by-name: get_audience_snapshot_by_name; list all: list_audience_snapshots. Read-only.",
      inputSchema: {
      "ID": z.string().optional().describe("ID (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ ID, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/configuration/audience-snapshot";
        const query: Record<string, string> = {};
      if (ID !== undefined) query["ID"] = String(ID);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_audience_snapshot", error);
      }
    },
  );
  registerTool(
    "get_audience_snapshot_by_name",
    {
      _meta: { endpoints: ["/client/configuration/audience-snapshot/by-name"] },
      title: "Get Audience Snapshot By Name",
      description: "Get one audience snapshot by its exact name. Same record as get_audience_snapshot keyed by name. Read-only.",
      inputSchema: {
      "Name": z.string().optional().describe("Name (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ Name, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/configuration/audience-snapshot/by-name";
        const query: Record<string, string> = {};
      if (Name !== undefined) query["Name"] = String(Name);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_audience_snapshot_by_name", error);
      }
    },
  );
  registerTool(
    "list_audience_snapshots",
    {
      _meta: { endpoints: ["/client/configuration/audience-snapshots"] },
      title: "List Audience Snapshots",
      description: "List all audience snapshots in the tenant (id + name). An audience snapshot is a point-in-time frozen membership of an audience — distinct from an audience definition (its structure) and the live audience file. Fetch one by id (get_audience_snapshot) or name (get_audience_snapshot_by_name). Read-only.",
      inputSchema: {
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/configuration/audience-snapshots";
        const query: Record<string, string> = {};
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in list_audience_snapshots", error);
      }
    },
  );
  registerTool(
    "get_cached_attributes",
    {
      _meta: { endpoints: ["/client/configuration/cached-attributes"] },
      title: "Get Cached Attributes",
      description: "Get a single cached-attribute configuration by its ID. Cached attributes are client-data attributes materialized in RPI's cache for fast selection/audience evaluation — NOT the same as an attribute list (a named grouping); this is the cache definition. By name: get_cached_attributes_by_name; to list all: list_cached_attributes. Read-only.",
      inputSchema: {
      "ID": z.string().optional().describe("ID (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ ID, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/configuration/cached-attributes";
        const query: Record<string, string> = {};
      if (ID !== undefined) query["ID"] = String(ID);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_cached_attributes", error);
      }
    },
  );
  registerTool(
    "get_cached_attributes_by_name",
    {
      _meta: { endpoints: ["/client/configuration/cached-attributes/by-name"] },
      title: "Get Cached Attributes By Name",
      description: "Get a single cached-attribute configuration by its exact name. Same record as get_cached_attributes but keyed by name. Cached attributes are the cache layer for fast rule evaluation, distinct from attribute lists. list_cached_attributes returns all of them. Read-only.",
      inputSchema: {
      "Name": z.string().optional().describe("Name (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ Name, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/configuration/cached-attributes/by-name";
        const query: Record<string, string> = {};
      if (Name !== undefined) query["Name"] = String(Name);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_cached_attributes_by_name", error);
      }
    },
  );
  registerTool(
    "list_cached_attributes",
    {
      _meta: { endpoints: ["/client/configuration/cached-attributes/collection"] },
      title: "List Cached Attributes",
      description: "List all cached-attribute configurations in the tenant (id + name of each). The browse/index form for the cached-attributes cache layer; fetch one with get_cached_attributes (by ID) or get_cached_attributes_by_name. Read-only.",
      inputSchema: {
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/configuration/cached-attributes/collection";
        const query: Record<string, string> = {};
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in list_cached_attributes", error);
      }
    },
  );
  registerTool(
    "get_data_extract_channel",
    {
      _meta: { endpoints: ["/client/configuration/channel/data-extract"] },
      title: "Get Data Extract Channel",
      description: "Get one DATA-EXTRACT channel's configuration by its ID — a channel that exports selected data to a file/destination. Distinct from list_channels (all channels of every type). Read-only.",
      inputSchema: {
      "ID": z.string().optional().describe("ID (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ ID, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/configuration/channel/data-extract";
        const query: Record<string, string> = {};
      if (ID !== undefined) query["ID"] = String(ID);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_data_extract_channel", error);
      }
    },
  );
  registerTool(
    "list_channels",
    {
      _meta: { endpoints: ["/client/configuration/channels"] },
      title: "List Channels",
      description: "List the delivery channels configured in the tenant (id + name). A channel is an output/delivery destination (email, SMS, file export, etc.). For a data-extract channel's full config use get_data_extract_channel. Read-only.",
      inputSchema: {
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/configuration/channels";
        const query: Record<string, string> = {};
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in list_channels", error);
      }
    },
  );
  registerTool(
    "search_column_mappings",
    {
      _meta: { endpoints: ["/client/configuration/data-management/column-mappings"] },
      title: "Search Column Mappings",
      description: "Search database column mappings (paged via PageNumber/PageSize), filtering by DatabaseID, Schema, TableName, and/or ColumnName. Column mappings link a database's physical columns to RPI attributes. Read-only.",
      inputSchema: {
      "PageNumber": z.string().optional().describe("PageNumber (query parameter)"),
      "PageSize": z.string().optional().describe("PageSize (query parameter)"),
      "DatabaseID": z.string().optional().describe("DatabaseID (query parameter)"),
      "Schema": z.string().optional().describe("Schema (query parameter)"),
      "TableName": z.string().optional().describe("TableName (query parameter)"),
      "ColumnName": z.string().optional().describe("ColumnName (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ PageNumber, PageSize, DatabaseID, Schema, TableName, ColumnName, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/configuration/data-management/column-mappings";
        const query: Record<string, string> = {};
      if (PageNumber !== undefined) query["PageNumber"] = String(PageNumber);
      if (PageSize !== undefined) query["PageSize"] = String(PageSize);
      if (DatabaseID !== undefined) query["DatabaseID"] = String(DatabaseID);
      if (Schema !== undefined) query["Schema"] = String(Schema);
      if (TableName !== undefined) query["TableName"] = String(TableName);
      if (ColumnName !== undefined) query["ColumnName"] = String(ColumnName);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in search_column_mappings", error);
      }
    },
  );
  registerTool(
    "get_configuration_parameter",
    {
      _meta: { endpoints: ["/client/configuration/parameter"] },
      title: "Get Configuration Parameter",
      description: "Get one configuration parameter's value by its EXACT name. For partial-name matching use search_configuration_parameters_by_name; for all use list_configuration_parameters. Read-only.",
      inputSchema: {
      "Name": z.string().optional().describe("Name (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ Name, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/configuration/parameter";
        const query: Record<string, string> = {};
      if (Name !== undefined) query["Name"] = String(Name);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_configuration_parameter", error);
      }
    },
  );
  registerTool(
    "list_configuration_parameters",
    {
      _meta: { endpoints: ["/client/configuration/parameters"] },
      title: "List Configuration Parameters",
      description: "List all configuration parameters (tenant-level settings) with their values. To fetch one by exact name use get_configuration_parameter; to find by partial name use search_configuration_parameters_by_name. Read-only.",
      inputSchema: {
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/configuration/parameters";
        const query: Record<string, string> = {};
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in list_configuration_parameters", error);
      }
    },
  );
  registerTool(
    "search_configuration_parameters_by_name",
    {
      _meta: { endpoints: ["/client/configuration/parameters/search-by-name"] },
      title: "Search Configuration Parameters By Name",
      description: "Search configuration parameters by a PARTIAL/substring name, returning all matches. Distinct from get_configuration_parameter (exact name, one) and list_configuration_parameters (all). Read-only.",
      inputSchema: {
      "Name": z.string().optional().describe("Name (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ Name, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/configuration/parameters/search-by-name";
        const query: Record<string, string> = {};
      if (Name !== undefined) query["Name"] = String(Name);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in search_configuration_parameters_by_name", error);
      }
    },
  );
  registerTool(
    "get_database",
    {
      _meta: { endpoints: ["/client/configuration/database"] },
      title: "Get Database",
      description: "Get one configured database by its ID — its connection/config metadata (NOT its schema). For the tables/columns use get_database_catalog; to browse all use list_databases. Read-only.",
      inputSchema: {
      "ID": z.string().optional().describe("ID (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ ID, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/configuration/database";
        const query: Record<string, string> = {};
      if (ID !== undefined) query["ID"] = String(ID);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_database", error);
      }
    },
  );
  registerTool(
    "get_database_catalog",
    {
      _meta: { endpoints: ["/client/configuration/database/catalog"] },
      title: "Get Database Catalog",
      description: "Get the CATALOG (schemas, tables, columns) of one database by its ID — the queryable structure. Distinct from get_database, which returns the database's connection config, not its schema. Read-only.",
      inputSchema: {
      "ID": z.string().optional().describe("ID (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ ID, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/configuration/database/catalog";
        const query: Record<string, string> = {};
      if (ID !== undefined) query["ID"] = String(ID);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_database_catalog", error);
      }
    },
  );
  registerTool(
    "get_database_key",
    {
      _meta: { endpoints: ["/client/configuration/database-key"] },
      title: "Get Database Key",
      description: "Get one database key definition by its ID. by-name: get_database_key_by_name; list all: list_database_keys. Distinct from get_database (the data source itself). Read-only.",
      inputSchema: {
      "ID": z.string().optional().describe("ID (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ ID, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/configuration/database-key";
        const query: Record<string, string> = {};
      if (ID !== undefined) query["ID"] = String(ID);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_database_key", error);
      }
    },
  );
  registerTool(
    "get_database_key_by_name",
    {
      _meta: { endpoints: ["/client/configuration/database-key/by-name"] },
      title: "Get Database Key By Name",
      description: "Get one database key definition by its exact name. Same record as get_database_key keyed by name; list_database_keys returns all. Read-only.",
      inputSchema: {
      "Name": z.string().optional().describe("Name (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ Name, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/configuration/database-key/by-name";
        const query: Record<string, string> = {};
      if (Name !== undefined) query["Name"] = String(Name);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_database_key_by_name", error);
      }
    },
  );
  registerTool(
    "list_database_keys",
    {
      _meta: { endpoints: ["/client/configuration/database-keys"] },
      title: "List Database Keys",
      description: "List all database key definitions in the tenant. A database key defines the primary/matching key for a database's records. Fetch one by id (get_database_key) or name (get_database_key_by_name). Read-only.",
      inputSchema: {
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/configuration/database-keys";
        const query: Record<string, string> = {};
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in list_database_keys", error);
      }
    },
  );
  registerTool(
    "list_databases",
    {
      _meta: { endpoints: ["/client/configuration/databases"] },
      title: "List Databases",
      description: "List all databases configured in the RPI tenant (id + name). A database is a configured data source RPI reads client data from. Fetch one's config with get_database, its schema with get_database_catalog. Read-only.",
      inputSchema: {
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/configuration/databases";
        const query: Record<string, string> = {};
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in list_databases", error);
      }
    },
  );
  registerTool(
    "get_configuration_file_attachment",
    {
      _meta: { endpoints: ["/client/configuration/file-attachment"] },
      title: "Get Configuration File Attachment",
      description: "Get metadata for one configuration file attachment by its ID (a file attached to a configuration object). Read-only.",
      inputSchema: {
      "ID": z.string().optional().describe("ID (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ ID, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/configuration/file-attachment";
        const query: Record<string, string> = {};
      if (ID !== undefined) query["ID"] = String(ID);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_configuration_file_attachment", error);
      }
    },
  );
  registerTool(
    "list_file_type_approvers",
    {
      _meta: { endpoints: ["/client/configuration/file-type-approval"] },
      title: "List File Type Approvers",
      description: "List the file-type approval configuration — which roles may approve which file types before use. Read-only.",
      inputSchema: {
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/configuration/file-type-approval";
        const query: Record<string, string> = {};
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in list_file_type_approvers", error);
      }
    },
  );
  registerTool(
    "get_ftp_location",
    {
      _meta: { endpoints: ["/client/configuration/ftp-location"] },
      title: "Get Ftp Location",
      description: "Get one FTP/SFTP location's configuration by its ID. by-name: get_ftp_location_by_name; list all: list_ftp_locations. Read-only.",
      inputSchema: {
      "ID": z.string().optional().describe("ID (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ ID, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/configuration/ftp-location";
        const query: Record<string, string> = {};
      if (ID !== undefined) query["ID"] = String(ID);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_ftp_location", error);
      }
    },
  );
  registerTool(
    "get_ftp_location_by_name",
    {
      _meta: { endpoints: ["/client/configuration/ftp-location/by-name"] },
      title: "Get Ftp Location By Name",
      description: "Get one FTP/SFTP location's configuration by its exact name. Same record as get_ftp_location keyed by name. Read-only.",
      inputSchema: {
      "Name": z.string().optional().describe("Name (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ Name, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/configuration/ftp-location/by-name";
        const query: Record<string, string> = {};
      if (Name !== undefined) query["Name"] = String(Name);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_ftp_location_by_name", error);
      }
    },
  );
  registerTool(
    "list_ftp_locations",
    {
      _meta: { endpoints: ["/client/configuration/ftp-locations"] },
      title: "List Ftp Locations",
      description: "List all FTP/SFTP locations configured in the tenant (id + name) — endpoints RPI reads/writes files to. Fetch one by id (get_ftp_location) or name (get_ftp_location_by_name). Read-only.",
      inputSchema: {
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/configuration/ftp-locations";
        const query: Record<string, string> = {};
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in list_ftp_locations", error);
      }
    },
  );
  registerTool(
    "get_managed_basic_list",
    {
      _meta: { endpoints: ["/client/configuration/managed-basic-list"] },
      title: "Get Managed Basic List",
      description: "Get one managed basic list by its ID, including its entries. A managed basic list is an admin-curated list of values used by Basic selection rules — distinct from an attribute list (attribute grouping) and a value list (allowed-value set). By name: get_managed_basic_list_by_name; browse all: list_managed_basic_lists. Read-only.",
      inputSchema: {
      "ID": z.string().optional().describe("ID (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ ID, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/configuration/managed-basic-list";
        const query: Record<string, string> = {};
      if (ID !== undefined) query["ID"] = String(ID);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_managed_basic_list", error);
      }
    },
  );
  registerTool(
    "get_managed_basic_list_by_name",
    {
      _meta: { endpoints: ["/client/configuration/managed-basic-list/by-name"] },
      title: "Get Managed Basic List By Name",
      description: "Get one managed basic list by its exact name, including its entries. Same record as get_managed_basic_list keyed by name. Admin-curated list for Basic selection rules; list_managed_basic_lists returns all. Read-only.",
      inputSchema: {
      "Name": z.string().optional().describe("Name (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ Name, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/configuration/managed-basic-list/by-name";
        const query: Record<string, string> = {};
      if (Name !== undefined) query["Name"] = String(Name);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_managed_basic_list_by_name", error);
      }
    },
  );
  registerTool(
    "list_managed_basic_lists",
    {
      _meta: { endpoints: ["/client/configuration/managed-basic-lists"] },
      title: "List Managed Basic Lists",
      description: "List all managed basic lists in the tenant (id + name of each). Browse/index form; fetch one with get_managed_basic_list (by ID) or get_managed_basic_list_by_name. Read-only.",
      inputSchema: {
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/configuration/managed-basic-lists";
        const query: Record<string, string> = {};
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in list_managed_basic_lists", error);
      }
    },
  );
  registerTool(
    "get_resolution_level",
    {
      _meta: { endpoints: ["/client/configuration/resolution-level"] },
      title: "Get Resolution Level",
      description: "Get one identity resolution level by its ID. by-name: get_resolution_level_by_name; list all: list_resolution_levels. Read-only.",
      inputSchema: {
      "ID": z.string().optional().describe("ID (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ ID, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/configuration/resolution-level";
        const query: Record<string, string> = {};
      if (ID !== undefined) query["ID"] = String(ID);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_resolution_level", error);
      }
    },
  );
  registerTool(
    "get_resolution_level_by_name",
    {
      _meta: { endpoints: ["/client/configuration/resolution-level/by-name"] },
      title: "Get Resolution Level By Name",
      description: "Get one identity resolution level by its exact name. Same record as get_resolution_level keyed by name. Read-only.",
      inputSchema: {
      "Name": z.string().optional().describe("Name (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ Name, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/configuration/resolution-level/by-name";
        const query: Record<string, string> = {};
      if (Name !== undefined) query["Name"] = String(Name);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_resolution_level_by_name", error);
      }
    },
  );
  registerTool(
    "list_resolution_levels",
    {
      _meta: { endpoints: ["/client/configuration/resolution-levels"] },
      title: "List Resolution Levels",
      description: "List all identity resolution levels in the tenant (id + name). A resolution level is a tier of identity matching (e.g. individual, household). Fetch one by id (get_resolution_level) or name (get_resolution_level_by_name). Read-only.",
      inputSchema: {
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/configuration/resolution-levels";
        const query: Record<string, string> = {};
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in list_resolution_levels", error);
      }
    },
  );
  registerTool(
    "get_seed_definition",
    {
      _meta: { endpoints: ["/client/configuration/seed-definition"] },
      title: "Get Seed Definition",
      description: "Get one seed definition by its ID, including its seed records. list all: list_seed_definitions. Read-only.",
      inputSchema: {
      "ID": z.string().optional().describe("ID (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ ID, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/configuration/seed-definition";
        const query: Record<string, string> = {};
      if (ID !== undefined) query["ID"] = String(ID);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_seed_definition", error);
      }
    },
  );
  registerTool(
    "list_seed_definitions",
    {
      _meta: { endpoints: ["/client/configuration/seed-definitions"] },
      title: "List Seed Definitions",
      description: "List all seed definitions in the tenant (id + name). A seed definition specifies seed records (test/monitoring addresses) injected into campaign outputs. Fetch one with get_seed_definition. Read-only.",
      inputSchema: {
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/configuration/seed-definitions";
        const query: Record<string, string> = {};
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in list_seed_definitions", error);
      }
    },
  );
  registerTool(
    "get_single_customer_view",
    {
      _meta: { endpoints: ["/client/configuration/single-customer-view"] },
      title: "Get Single Customer View",
      description: "Get one single customer view (SCV) by its ID, including its structure. by-name: get_single_customer_view_by_name; for a specific attribute group within it: get_single_customer_view_attribute_group. Read-only.",
      inputSchema: {
      "ID": z.string().optional().describe("ID (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ ID, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/configuration/single-customer-view";
        const query: Record<string, string> = {};
      if (ID !== undefined) query["ID"] = String(ID);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_single_customer_view", error);
      }
    },
  );
  registerTool(
    "get_single_customer_view_by_name",
    {
      _meta: { endpoints: ["/client/configuration/single-customer-view/by-name"] },
      title: "Get Single Customer View By Name",
      description: "Get one single customer view (SCV) by its exact name. Same record as get_single_customer_view keyed by name. Read-only.",
      inputSchema: {
      "Name": z.string().optional().describe("Name (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ Name, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/configuration/single-customer-view/by-name";
        const query: Record<string, string> = {};
      if (Name !== undefined) query["Name"] = String(Name);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_single_customer_view_by_name", error);
      }
    },
  );
  registerTool(
    "get_single_customer_view_attribute_group",
    {
      _meta: { endpoints: ["/client/configuration/single-customer-view/attribute-group"] },
      title: "Get Single Customer View Attribute Group",
      description: "Get one attribute group WITHIN a single customer view, identified by the view's ID plus the group's ResourceId. An attribute group organizes related attributes inside an SCV. by-name variant: get_single_customer_view_attribute_group_by_name. Read-only.",
      inputSchema: {
      "ID": z.string().optional().describe("ID (query parameter)"),
      "ResourceId": z.string().optional().describe("ResourceId (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ ID, ResourceId, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/configuration/single-customer-view/attribute-group";
        const query: Record<string, string> = {};
      if (ID !== undefined) query["ID"] = String(ID);
      if (ResourceId !== undefined) query["ResourceId"] = String(ResourceId);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_single_customer_view_attribute_group", error);
      }
    },
  );
  registerTool(
    "get_single_customer_view_attribute_group_by_name",
    {
      _meta: { endpoints: ["/client/configuration/single-customer-view/attribute-group/by-name"] },
      title: "Get Single Customer View Attribute Group By Name",
      description: "Get one attribute group within a single customer view, identified by the view's Name plus the group's ResourceName. Same record as get_single_customer_view_attribute_group keyed by names instead of IDs. Read-only.",
      inputSchema: {
      "Name": z.string().optional().describe("Name (query parameter)"),
      "ResourceName": z.string().optional().describe("ResourceName (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ Name, ResourceName, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/configuration/single-customer-view/attribute-group/by-name";
        const query: Record<string, string> = {};
      if (Name !== undefined) query["Name"] = String(Name);
      if (ResourceName !== undefined) query["ResourceName"] = String(ResourceName);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_single_customer_view_attribute_group_by_name", error);
      }
    },
  );
  registerTool(
    "get_single_customer_view_event_details_html",
    {
      _meta: { endpoints: ["/client/single-customer-view/event-details-as-html"] },
      title: "Get Single Customer View Event Details Html",
      description: "Get a single-customer-view event's details rendered as HTML — the detail card for one channel-execution event (ChannelExecutionID + OfferTemplateInstanceID + OfferCode). Relates to the single-customer-view definition reads (get_single_customer_view). Read-only.",
      inputSchema: {
      "ChannelExecutionID": z.string().optional().describe("ChannelExecutionID (query parameter)"),
      "OfferTemplateInstanceID": z.string().optional().describe("OfferTemplateInstanceID (query parameter)"),
      "OfferCode": z.string().optional().describe("OfferCode (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ ChannelExecutionID, OfferTemplateInstanceID, OfferCode, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/single-customer-view/event-details-as-html";
        const query: Record<string, string> = {};
      if (ChannelExecutionID !== undefined) query["ChannelExecutionID"] = String(ChannelExecutionID);
      if (OfferTemplateInstanceID !== undefined) query["OfferTemplateInstanceID"] = String(OfferTemplateInstanceID);
      if (OfferCode !== undefined) query["OfferCode"] = String(OfferCode);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_single_customer_view_event_details_html", error);
      }
    },
  );
  registerTool(
    "list_single_customer_views",
    {
      _meta: { endpoints: ["/client/configuration/single-customer-views"] },
      title: "List Single Customer Views",
      description: "List all single customer views (SCVs) in the tenant (id + name). An SCV is a unified customer data model spanning databases. Fetch one by id (get_single_customer_view) or name (get_single_customer_view_by_name). Read-only.",
      inputSchema: {
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/configuration/single-customer-views";
        const query: Record<string, string> = {};
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in list_single_customer_views", error);
      }
    },
  );
  registerTool(
    "get_sql_database_definition",
    {
      _meta: { endpoints: ["/client/configuration/sql-database-definition"] },
      title: "Get Sql Database Definition",
      description: "Get one SQL database definition by its ID, including its SQL. by-name: get_sql_database_definition_by_name; for what it can be queried on use get_sql_database_definition_available_criterion. Read-only.",
      inputSchema: {
      "ID": z.string().optional().describe("ID (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ ID, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/configuration/sql-database-definition";
        const query: Record<string, string> = {};
      if (ID !== undefined) query["ID"] = String(ID);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_sql_database_definition", error);
      }
    },
  );
  registerTool(
    "get_sql_database_definition_by_name",
    {
      _meta: { endpoints: ["/client/configuration/sql-database-definition/by-name"] },
      title: "Get Sql Database Definition By Name",
      description: "Get one SQL database definition by its exact name. Same record as get_sql_database_definition keyed by name. Read-only.",
      inputSchema: {
      "Name": z.string().optional().describe("Name (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ Name, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/configuration/sql-database-definition/by-name";
        const query: Record<string, string> = {};
      if (Name !== undefined) query["Name"] = String(Name);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_sql_database_definition_by_name", error);
      }
    },
  );
  registerTool(
    "get_sql_database_definition_available_criterion",
    {
      _meta: { endpoints: ["/client/configuration/sql-database-definition/available-criterion"] },
      title: "Get Sql Database Definition Available Criterion",
      description: "Get the available selection criteria (queryable fields/operators) for one SQL database definition by its ID — what you can build rules against. Distinct from get_sql_database_definition (the definition itself). Read-only.",
      inputSchema: {
      "ID": z.string().optional().describe("ID (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ ID, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/configuration/sql-database-definition/available-criterion";
        const query: Record<string, string> = {};
      if (ID !== undefined) query["ID"] = String(ID);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_sql_database_definition_available_criterion", error);
      }
    },
  );
  registerTool(
    "list_sql_database_definitions",
    {
      _meta: { endpoints: ["/client/configuration/sql-database-definitions"] },
      title: "List Sql Database Definitions",
      description: "List SQL database definitions in the tenant (full form). A SQL database definition is a data source defined by a SQL query rather than a physical table. For a lighter id/name index use list_sql_database_definitions_summary. Read-only.",
      inputSchema: {
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/configuration/sql-database-definitions";
        const query: Record<string, string> = {};
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in list_sql_database_definitions", error);
      }
    },
  );
  registerTool(
    "list_sql_database_definitions_summary",
    {
      _meta: { endpoints: ["/client/configuration/sql-database-definitions/summary"] },
      title: "List Sql Database Definitions Summary",
      description: "List SQL database definitions in SUMMARY form (id/name only) — the lightweight index vs the fuller list_sql_database_definitions. Use to browse before fetching one. Read-only.",
      inputSchema: {
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/configuration/sql-database-definitions/summary";
        const query: Record<string, string> = {};
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in list_sql_database_definitions_summary", error);
      }
    },
  );
  registerTool(
    "get_table_join_multiple",
    {
      _meta: { endpoints: ["/client/configuration/table-join-multiple"] },
      title: "Get Table Join Multiple",
      description: "Get one MULTIPLE table-join definition by its ID — a composite/multi-key join. Distinct from get_table_join_simple (single-key). by-name: get_table_join_multiple_by_name. Read-only.",
      inputSchema: {
      "ID": z.string().optional().describe("ID (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ ID, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/configuration/table-join-multiple";
        const query: Record<string, string> = {};
      if (ID !== undefined) query["ID"] = String(ID);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_table_join_multiple", error);
      }
    },
  );
  registerTool(
    "get_table_join_multiple_by_name",
    {
      _meta: { endpoints: ["/client/configuration/table-join-multiple/by-name"] },
      title: "Get Table Join Multiple By Name",
      description: "Get one multiple (composite-key) table-join definition by its exact name. Same record as get_table_join_multiple keyed by name. Read-only.",
      inputSchema: {
      "Name": z.string().optional().describe("Name (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ Name, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/configuration/table-join-multiple/by-name";
        const query: Record<string, string> = {};
      if (Name !== undefined) query["Name"] = String(Name);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_table_join_multiple_by_name", error);
      }
    },
  );
  registerTool(
    "list_table_joins",
    {
      _meta: { endpoints: ["/client/configuration/table-joins"] },
      title: "List Table Joins",
      description: "List all table-join definitions in the tenant (both simple and multiple). A table join relates two database tables for querying. Fetch a specific one with get_table_join_simple or get_table_join_multiple. Read-only.",
      inputSchema: {
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/configuration/table-joins";
        const query: Record<string, string> = {};
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in list_table_joins", error);
      }
    },
  );
  registerTool(
    "get_table_join_simple",
    {
      _meta: { endpoints: ["/client/configuration/table-join-simple"] },
      title: "Get Table Join Simple",
      description: "Get one SIMPLE table-join definition by its ID — a single-key join between two tables. Distinct from get_table_join_multiple (composite/multi-key). by-name: get_table_join_simple_by_name. Read-only.",
      inputSchema: {
      "ID": z.string().optional().describe("ID (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ ID, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/configuration/table-join-simple";
        const query: Record<string, string> = {};
      if (ID !== undefined) query["ID"] = String(ID);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_table_join_simple", error);
      }
    },
  );
  registerTool(
    "get_table_join_simple_by_name",
    {
      _meta: { endpoints: ["/client/configuration/table-join-simple/by-name"] },
      title: "Get Table Join Simple By Name",
      description: "Get one simple (single-key) table-join definition by its exact name. Same record as get_table_join_simple keyed by name. Read-only.",
      inputSchema: {
      "Name": z.string().optional().describe("Name (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ Name, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/configuration/table-join-simple/by-name";
        const query: Record<string, string> = {};
      if (Name !== undefined) query["Name"] = String(Name);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_table_join_simple_by_name", error);
      }
    },
  );
  registerTool(
    "get_user_details",
    {
      _meta: { endpoints: ["/client/configuration/user/details"] },
      title: "Get User Details",
      description: "Get the current client user's configuration details including group memberships (pass OnlyActiveGroups=true to include only active groups). The config view of the caller — distinct from get_user_profile (auth identity/roles) and get_user_permissions (a named user's permissions). Read-only.",
      inputSchema: {
      "OnlyActiveGroups": z.string().optional().describe("OnlyActiveGroups (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ OnlyActiveGroups, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/configuration/user/details";
        const query: Record<string, string> = {};
      if (OnlyActiveGroups !== undefined) query["OnlyActiveGroups"] = String(OnlyActiveGroups);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_user_details", error);
      }
    },
  );
  registerTool(
    "get_user_group",
    {
      _meta: { endpoints: ["/client/configuration/user-group"] },
      title: "Get User Group",
      description: "Get one user group by its ID, including its members. by-name: get_user_group_by_name; list all: list_user_groups. Read-only.",
      inputSchema: {
      "ID": z.string().optional().describe("ID (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ ID, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/configuration/user-group";
        const query: Record<string, string> = {};
      if (ID !== undefined) query["ID"] = String(ID);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_user_group", error);
      }
    },
  );
  registerTool(
    "get_user_group_by_name",
    {
      _meta: { endpoints: ["/client/configuration/user-group/by-name"] },
      title: "Get User Group By Name",
      description: "Get one user group by its exact name, including its members. Same record as get_user_group keyed by name. Read-only.",
      inputSchema: {
      "Name": z.string().optional().describe("Name (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ Name, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/configuration/user-group/by-name";
        const query: Record<string, string> = {};
      if (Name !== undefined) query["Name"] = String(Name);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_user_group_by_name", error);
      }
    },
  );
  registerTool(
    "list_user_groups",
    {
      _meta: { endpoints: ["/client/configuration/user-groups"] },
      title: "List User Groups",
      description: "List all user groups in the tenant (id + name). User groups bundle users for permission assignment. Fetch one by id (get_user_group) or name (get_user_group_by_name). Read-only.",
      inputSchema: {
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/configuration/user-groups";
        const query: Record<string, string> = {};
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in list_user_groups", error);
      }
    },
  );
  registerTool(
    "get_user_permissions",
    {
      _meta: { endpoints: ["/client/configuration/user-permissions"] },
      title: "Get User Permissions",
      description: "Get the permissions assigned to a specific user, identified by name. Distinct from list_available_permissions (the full catalog of assignable permissions) and get_user_details (the caller's own config). Read-only.",
      inputSchema: {
      "Name": z.string().optional().describe("Name (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ Name, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/configuration/user-permissions";
        const query: Record<string, string> = {};
      if (Name !== undefined) query["Name"] = String(Name);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_user_permissions", error);
      }
    },
  );
  registerTool(
    "list_available_permissions",
    {
      _meta: { endpoints: ["/client/configuration/user-permissions/all-available"] },
      title: "List Available Permissions",
      description: "List every permission that CAN be assigned to users in the tenant (the catalog). Distinct from get_user_permissions, which returns what one named user actually has. Read-only.",
      inputSchema: {
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/configuration/user-permissions/all-available";
        const query: Record<string, string> = {};
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in list_available_permissions", error);
      }
    },
  );
  registerTool(
    "list_users",
    {
      _meta: { endpoints: ["/client/configuration/users"] },
      title: "List Users",
      description: "List the users configured in the RPI tenant (id + name). The tenant-administration view of users — distinct from get_user_profile (the auth identity of the current caller). Read-only.",
      inputSchema: {
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/configuration/users";
        const query: Record<string, string> = {};
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in list_users", error);
      }
    },
  );
  registerTool(
    "list_value_lists",
    {
      _meta: { endpoints: ["/client/configuration/value-lists"] },
      title: "List Value Lists",
      description: "List the value lists configured in the tenant. A value list is a defined set of allowed/enumerated values for an attribute (e.g. valid status or region codes) — distinct from an attribute list (a grouping of attributes) and a managed basic list (values for Basic rules). Read-only.",
      inputSchema: {
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/configuration/value-lists";
        const query: Record<string, string> = {};
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in list_value_lists", error);
      }
    },
  );
  registerTool(
    "get_web_adapter",
    {
      _meta: { endpoints: ["/client/configuration/web-adapter"] },
      title: "Get Web Adapter",
      description: "Get one web adapter's configuration by its ID. by-name: get_web_adapter_by_name; list all: list_web_adapters. Read-only.",
      inputSchema: {
      "ID": z.string().optional().describe("ID (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ ID, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/configuration/web-adapter";
        const query: Record<string, string> = {};
      if (ID !== undefined) query["ID"] = String(ID);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_web_adapter", error);
      }
    },
  );
  registerTool(
    "get_web_adapter_by_name",
    {
      _meta: { endpoints: ["/client/configuration/web-adapter/by-name"] },
      title: "Get Web Adapter By Name",
      description: "Get one web adapter's configuration by its exact name. Same record as get_web_adapter keyed by name. Read-only.",
      inputSchema: {
      "Name": z.string().optional().describe("Name (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ Name, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/configuration/web-adapter/by-name";
        const query: Record<string, string> = {};
      if (Name !== undefined) query["Name"] = String(Name);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_web_adapter_by_name", error);
      }
    },
  );
  registerTool(
    "list_web_adapters",
    {
      _meta: { endpoints: ["/client/configuration/web-adapters"] },
      title: "List Web Adapters",
      description: "List all web adapters configured in the tenant (id + name). A web adapter is a configured HTTP/web integration endpoint. Fetch one by id (get_web_adapter) or name (get_web_adapter_by_name). Read-only.",
      inputSchema: {
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/configuration/web-adapters";
        const query: Record<string, string> = {};
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in list_web_adapters", error);
      }
    },
  );
  registerTool(
    "get_web_publish_site_map",
    {
      _meta: { endpoints: ["/client/configuration/web-publish-site-map"] },
      title: "Get Web Publish Site Map",
      description: "Get one web-publish site map by its ID. by-name: get_web_publish_site_map_by_name; list all: list_web_publish_site_maps. Read-only.",
      inputSchema: {
      "ID": z.string().optional().describe("ID (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ ID, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/configuration/web-publish-site-map";
        const query: Record<string, string> = {};
      if (ID !== undefined) query["ID"] = String(ID);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_web_publish_site_map", error);
      }
    },
  );
  registerTool(
    "get_web_publish_site_map_by_name",
    {
      _meta: { endpoints: ["/client/configuration/web-publish-site-map/by-name"] },
      title: "Get Web Publish Site Map By Name",
      description: "Get one web-publish site map by its exact name. Same record as get_web_publish_site_map keyed by name. Read-only.",
      inputSchema: {
      "Name": z.string().optional().describe("Name (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ Name, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/configuration/web-publish-site-map/by-name";
        const query: Record<string, string> = {};
      if (Name !== undefined) query["Name"] = String(Name);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_web_publish_site_map_by_name", error);
      }
    },
  );
  registerTool(
    "list_web_publish_site_maps",
    {
      _meta: { endpoints: ["/client/configuration/web-publish-site-maps"] },
      title: "List Web Publish Site Maps",
      description: "List all web-publish site maps in the tenant (id + name). A site map configures a web-publishing destination's structure. Fetch one by id (get_web_publish_site_map) or name (get_web_publish_site_map_by_name). Read-only.",
      inputSchema: {
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/configuration/web-publish-site-maps";
        const query: Record<string, string> = {};
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in list_web_publish_site_maps", error);
      }
    },
  );
}
