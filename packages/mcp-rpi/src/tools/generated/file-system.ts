// AUTO-GENERATED by scripts/generate-tools.ts — DO NOT EDIT.
// In-scope RPI GET endpoints (#27634). Regenerate: bun run generate:tools
// All tools are read-only (GET); descriptions come from src/tools/overlay.ts.

import { z } from "zod";
import type { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js";
import type { RPIApiClient } from "../../client/rpi-api.js";
import { createToolRegistrar } from "../../tool-categories.js";
import { clientIdSchema, verboseSchema, jsonContent, errorContent, READ_ONLY_ANNOTATIONS, targetUrlOf } from "../generated-shared.js";

export function registerFileSystemGeneratedTools(server: McpServer, rpiClient: RPIApiClient): void {
  const registerTool = createToolRegistrar(server, "file-system");
  void rpiClient;
  registerTool(
    "search_external_folder_connectors",
    {
      _meta: { endpoints: ["/client/file-system/external/folders/search/connector-details"] },
      title: "Search External Folder Connectors",
      description: "Search external-folder connector details by name or ID (NameOrID) — the storage connectors (e.g. cloud/SFTP endpoints) that back external folders. External-storage connector infrastructure, distinct from the internal browsable folder tree (folders category). Read-only.",
      inputSchema: {
      "NameOrID": z.string().optional().describe("NameOrID (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ NameOrID, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/file-system/external/folders/search/connector-details";
        const query: Record<string, string> = {};
      if (NameOrID !== undefined) query["NameOrID"] = String(NameOrID);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in search_external_folder_connectors", error);
      }
    },
  );
  registerTool(
    "list_file_info_attribute_lists",
    {
      _meta: { endpoints: ["/client/file-system/file-info/attribute-lists"] },
      title: "List File Info Attribute Lists",
      description: "List the attribute lists available in the file-system file-info context (e.g. selectable as file-info display columns) — a catalog, no ID. Distinct from list_attribute_lists (every attribute list in the tenant — configuration domain) and from list_attribute_list_decision_rule_lists (files domain). Read-only.",
      inputSchema: {
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/file-system/file-info/attribute-lists";
        const query: Record<string, string> = {};
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in list_file_info_attribute_lists", error);
      }
    },
  );
  registerTool(
    "get_file_metadata",
    {
      _meta: { endpoints: ["/client/file-system/file-info/metadata"] },
      title: "Get File Metadata",
      description: "Get the metadata record for a stored file by its ID — the file-system properties of a file of ANY type (audience, interaction, rule, offer, …). This is metadata ABOUT the file, NOT its content (fetch content via the domain tool, e.g. get_offer / get_audience_by_id) and NOT the basic name/type/folder card (that's the get_file_info_by_id hand tool). Read-only.",
      inputSchema: {
      "ID": z.string().optional().describe("ID (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ ID, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/file-system/file-info/metadata";
        const query: Record<string, string> = {};
      if (ID !== undefined) query["ID"] = String(ID);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_file_metadata", error);
      }
    },
  );
  registerTool(
    "get_file_dependencies",
    {
      _meta: { endpoints: ["/client/file-system/file-info/file-dynamic-dependencies"] },
      title: "Get File Dependencies",
      description: "List the files a given file (ID) depends ON — its own inputs/references (outgoing / forward dependencies). The OPPOSITE direction — what depends on this file — is get_file_dependents. Read-only.",
      inputSchema: {
      "ID": z.string().optional().describe("ID (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ ID, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/file-system/file-info/file-dynamic-dependencies";
        const query: Record<string, string> = {};
      if (ID !== undefined) query["ID"] = String(ID);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_file_dependencies", error);
      }
    },
  );
  registerTool(
    "get_file_dependents",
    {
      _meta: { endpoints: ["/client/file-system/file-info/dependencies-on-file"] },
      title: "Get File Dependents",
      description: "List the files that depend ON a given file (ID) — what references it and would be affected if it changed (incoming / reverse dependencies). Pass IncludeLooseDependencies to include indirect references. The OPPOSITE direction — what this file itself depends on — is get_file_dependencies. Read-only.",
      inputSchema: {
      "ID": z.string().optional().describe("ID (query parameter)"),
      "IncludeLooseDependencies": z.string().optional().describe("IncludeLooseDependencies (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ ID, IncludeLooseDependencies, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/file-system/file-info/dependencies-on-file";
        const query: Record<string, string> = {};
      if (ID !== undefined) query["ID"] = String(ID);
      if (IncludeLooseDependencies !== undefined) query["IncludeLooseDependencies"] = String(IncludeLooseDependencies);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_file_dependents", error);
      }
    },
  );
  registerTool(
    "get_file_history",
    {
      _meta: { endpoints: ["/client/file-system/file-info/history"] },
      title: "Get File History",
      description: "Get the change history of a stored file by its ID — its edit/version trail. Companion to get_file_metadata (current properties) and the get_file_info_by_id hand tool (the name/type/folder card). Read-only.",
      inputSchema: {
      "ID": z.string().optional().describe("ID (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ ID, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/file-system/file-info/history";
        const query: Record<string, string> = {};
      if (ID !== undefined) query["ID"] = String(ID);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_file_history", error);
      }
    },
  );
}
