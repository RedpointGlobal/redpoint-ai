// AUTO-GENERATED by scripts/generate-tools.ts — DO NOT EDIT.
// In-scope RPI GET endpoints (#27634). Regenerate: bun run generate:tools
// All tools are read-only (GET); descriptions come from src/tools/overlay.ts.

import { z } from "zod";
import type { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js";
import type { RPIApiClient } from "../../client/rpi-api.js";
import { createToolRegistrar } from "../../tool-categories.js";
import { clientIdSchema, verboseSchema, jsonContent, errorContent, READ_ONLY_ANNOTATIONS, targetUrlOf } from "../generated-shared.js";

export function registerOperationsGeneratedTools(server: McpServer, rpiClient: RPIApiClient): void {
  const registerTool = createToolRegistrar(server, "operations");
  void rpiClient;
  registerTool(
    "get_system_health_monitoring_overview",
    {
      _meta: { endpoints: ["/client/operations/system-health/health-monitoring-overview"] },
      title: "Get System Health Monitoring Overview",
      description: "Get the client's system-health monitoring overview — an aggregate health/status view (pass RunCheckNow to force a fresh check; SkipIfNotAvailable to skip checks that are unavailable). Distinct from the get_system_health_availability hand tool (the cluster availability probe). Read-only.",
      inputSchema: {
      "RunCheckNow": z.string().optional().describe("RunCheckNow (query parameter)"),
      "SkipIfNotAvailable": z.string().optional().describe("SkipIfNotAvailable (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ RunCheckNow, SkipIfNotAvailable, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/operations/system-health/health-monitoring-overview";
        const query: Record<string, string> = {};
      if (RunCheckNow !== undefined) query["RunCheckNow"] = String(RunCheckNow);
      if (SkipIfNotAvailable !== undefined) query["SkipIfNotAvailable"] = String(SkipIfNotAvailable);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_system_health_monitoring_overview", error);
      }
    },
  );
  registerTool(
    "get_audience_snapshot_workflows_status",
    {
      _meta: { endpoints: ["/client/operations/snapshots/status"] },
      title: "Get Audience Snapshot Workflows Status",
      description: "Get the status of the client's audience-snapshot workflows — whether snapshot refresh runs are pending/running/complete. About the snapshot RUNS; the snapshots themselves are get_audience_snapshot / list_audience_snapshots (configuration domain). Read-only.",
      inputSchema: {
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/operations/snapshots/status";
        const query: Record<string, string> = {};
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_audience_snapshot_workflows_status", error);
      }
    },
  );
  registerTool(
    "get_operation_sql_audit_history",
    {
      _meta: { endpoints: ["/client/operations/logs/sql-audit-history"] },
      title: "Get Operation Sql Audit History",
      description: "Get the CLIENT's SQL audit history (paged) — audited SQL statements executed for this client. The general (non-SQL) client audit trail is get_operation_audit_history. Read-only.",
      inputSchema: {
      "PageNumber": z.string().optional().describe("PageNumber (query parameter)"),
      "PageSize": z.string().optional().describe("PageSize (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ PageNumber, PageSize, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/operations/logs/sql-audit-history";
        const query: Record<string, string> = {};
      if (PageNumber !== undefined) query["PageNumber"] = String(PageNumber);
      if (PageSize !== undefined) query["PageSize"] = String(PageSize);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_operation_sql_audit_history", error);
      }
    },
  );
  registerTool(
    "list_operation_system_tasks",
    {
      _meta: { endpoints: ["/client/operations/system-tasks"] },
      title: "List Operation System Tasks",
      description: "List the CLIENT's system tasks — background/scheduled jobs scoped to this client and their state. The CLUSTER-wide equivalent is get_cluster_system_tasks. Read-only.",
      inputSchema: {
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/operations/system-tasks";
        const query: Record<string, string> = {};
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in list_operation_system_tasks", error);
      }
    },
  );
  registerTool(
    "get_operation_housekeeping_logs",
    {
      _meta: { endpoints: ["/client/operations/logs/housekeeping-logs"] },
      title: "Get Operation Housekeeping Logs",
      description: "Get the CLIENT's housekeeping (maintenance-job) logs (paged) — cleanup/retention jobs scoped to this client. The CLUSTER-wide equivalent is get_cluster_housekeeping_log. Read-only.",
      inputSchema: {
      "PageNumber": z.string().optional().describe("PageNumber (query parameter)"),
      "PageSize": z.string().optional().describe("PageSize (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ PageNumber, PageSize, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/operations/logs/housekeeping-logs";
        const query: Record<string, string> = {};
      if (PageNumber !== undefined) query["PageNumber"] = String(PageNumber);
      if (PageSize !== undefined) query["PageSize"] = String(PageSize);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_operation_housekeeping_logs", error);
      }
    },
  );
  registerTool(
    "get_operation_audit_history",
    {
      _meta: { endpoints: ["/client/operations/logs/audit-history"] },
      title: "Get Operation Audit History",
      description: "Get the CLIENT's audit history (paged, PageNumber/PageSize) — the change/action audit trail scoped to this client/tenant. SQL-statement-level audit is get_operation_sql_audit_history; the CLUSTER-wide audit trail is the get_cluster_audit_history hand tool. Read-only.",
      inputSchema: {
      "PageNumber": z.string().optional().describe("PageNumber (query parameter)"),
      "PageSize": z.string().optional().describe("PageSize (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ PageNumber, PageSize, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/operations/logs/audit-history";
        const query: Record<string, string> = {};
      if (PageNumber !== undefined) query["PageNumber"] = String(PageNumber);
      if (PageSize !== undefined) query["PageSize"] = String(PageSize);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_operation_audit_history", error);
      }
    },
  );
  registerTool(
    "list_execution_services",
    {
      _meta: { endpoints: ["/client/operations/execution-services"] },
      title: "List Execution Services",
      description: "List the execution services available to the client — the compute/execution endpoints that run the client's workflows and jobs, with their status. No ID. Read-only.",
      inputSchema: {
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/operations/execution-services";
        const query: Record<string, string> = {};
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in list_execution_services", error);
      }
    },
  );
}
