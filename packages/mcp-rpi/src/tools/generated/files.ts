// AUTO-GENERATED by scripts/generate-tools.ts — DO NOT EDIT.
// In-scope RPI GET endpoints (#27634). Regenerate: bun run generate:tools
// All tools are read-only (GET); descriptions come from src/tools/overlay.ts.

import { z } from "zod";
import type { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js";
import type { RPIApiClient } from "../../client/rpi-api.js";
import { createToolRegistrar } from "../../tool-categories.js";
import { clientIdSchema, verboseSchema, jsonContent, errorContent, READ_ONLY_ANNOTATIONS, targetUrlOf } from "../generated-shared.js";

export function registerFilesGeneratedTools(server: McpServer, rpiClient: RPIApiClient): void {
  const registerTool = createToolRegistrar(server, "files");
  void rpiClient;
  registerTool(
    "get_attribute_list_decision_rule",
    {
      _meta: { endpoints: ["/client/files/attribute-list-decision-rule"] },
      title: "Get Attribute List Decision Rule",
      description: "Get one attribute-list decision rule by ID — a decision rule that selects an outcome from an attribute list. This is the RULE, not the attribute list itself (get_attribute_list). Its usable attribute lists are given by list_attribute_list_decision_rule_lists. Siblings: database, json, web, orchestration decision rules. Read-only.",
      inputSchema: {
      "ID": z.string().optional().describe("ID (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ ID, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/files/attribute-list-decision-rule";
        const query: Record<string, string> = {};
      if (ID !== undefined) query["ID"] = String(ID);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_attribute_list_decision_rule", error);
      }
    },
  );
  registerTool(
    "list_attribute_list_decision_rule_lists",
    {
      _meta: { endpoints: ["/client/files/attribute-list-decision-rule/attribute-lists"] },
      title: "List Attribute List Decision Rule Lists",
      description: "List the attribute lists available for use inside attribute-list decision rules (catalog — no ID). Distinct from list_attribute_lists (every attribute list in the tenant); this is scoped to those valid as a decision-rule source. For the rule use get_attribute_list_decision_rule. Read-only.",
      inputSchema: {
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/files/attribute-list-decision-rule/attribute-lists";
        const query: Record<string, string> = {};
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in list_attribute_list_decision_rule_lists", error);
      }
    },
  );
  registerTool(
    "get_cell_list",
    {
      _meta: { endpoints: ["/client/files/cell-list"] },
      title: "Get Cell List",
      description: "Get one cell list by ID — the matrix of cells (audience × offer/treatment combinations) used in campaign contact planning. Read-only.",
      inputSchema: {
      "ID": z.string().optional().describe("ID (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ ID, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/files/cell-list";
        const query: Record<string, string> = {};
      if (ID !== undefined) query["ID"] = String(ID);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_cell_list", error);
      }
    },
  );
  registerTool(
    "get_chart_analysis_panel",
    {
      _meta: { endpoints: ["/client/files/analyze/chart-analysis"] },
      title: "Get Chart Analysis Panel",
      description: "Get one chart analysis panel by ID — a saved chart-style analysis over client data. Its selectable aggregation functions come from get_chart_analysis_panel_available_aggregations. Sibling panels: cross-tab (get_cross_tab_analysis_panel), venn (get_venn_analysis_panel). Read-only.",
      inputSchema: {
      "ID": z.string().optional().describe("ID (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ ID, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/files/analyze/chart-analysis";
        const query: Record<string, string> = {};
      if (ID !== undefined) query["ID"] = String(ID);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_chart_analysis_panel", error);
      }
    },
  );
  registerTool(
    "get_chart_analysis_panel_available_aggregations",
    {
      _meta: { endpoints: ["/client/files/analyze/chart-analysis/available-aggregations"] },
      title: "Get Chart Analysis Panel Available Aggregations",
      description: "List the aggregation functions a chart analysis panel can apply for a given attribute (AttributeID) and function attribute (FunctionAttributeID). The cross-tab equivalent is get_cross_tab_analysis_panel_available_aggregations. Read-only.",
      inputSchema: {
      "AttributeID": z.string().optional().describe("AttributeID (query parameter)"),
      "FunctionAttributeID": z.string().optional().describe("FunctionAttributeID (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ AttributeID, FunctionAttributeID, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/files/analyze/chart-analysis/available-aggregations";
        const query: Record<string, string> = {};
      if (AttributeID !== undefined) query["AttributeID"] = String(AttributeID);
      if (FunctionAttributeID !== undefined) query["FunctionAttributeID"] = String(FunctionAttributeID);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_chart_analysis_panel_available_aggregations", error);
      }
    },
  );
  registerTool(
    "get_cross_tab_analysis_panel",
    {
      _meta: { endpoints: ["/client/files/analyze/cross-tab-analysis"] },
      title: "Get Cross Tab Analysis Panel",
      description: "Get one cross-tab (pivot) analysis panel by ID — a row/column cross-tabulation over client data. Its aggregations: get_cross_tab_analysis_panel_available_aggregations; its cell palette: get_cross_tab_analysis_panel_predefined_colors. Siblings: chart, venn analysis panels. Read-only.",
      inputSchema: {
      "ID": z.string().optional().describe("ID (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ ID, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/files/analyze/cross-tab-analysis";
        const query: Record<string, string> = {};
      if (ID !== undefined) query["ID"] = String(ID);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_cross_tab_analysis_panel", error);
      }
    },
  );
  registerTool(
    "get_cross_tab_analysis_panel_available_aggregations",
    {
      _meta: { endpoints: ["/client/files/analyze/cross-tab-analysis/available-aggregations"] },
      title: "Get Cross Tab Analysis Panel Available Aggregations",
      description: "List the aggregation functions available to a cross-tab analysis panel for the given attribute (AttributeID) and function attribute (FunctionAttributeID). The chart equivalent is get_chart_analysis_panel_available_aggregations. Read-only.",
      inputSchema: {
      "AttributeID": z.string().optional().describe("AttributeID (query parameter)"),
      "FunctionAttributeID": z.string().optional().describe("FunctionAttributeID (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ AttributeID, FunctionAttributeID, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/files/analyze/cross-tab-analysis/available-aggregations";
        const query: Record<string, string> = {};
      if (AttributeID !== undefined) query["AttributeID"] = String(AttributeID);
      if (FunctionAttributeID !== undefined) query["FunctionAttributeID"] = String(FunctionAttributeID);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_cross_tab_analysis_panel_available_aggregations", error);
      }
    },
  );
  registerTool(
    "get_cross_tab_analysis_panel_predefined_colors",
    {
      _meta: { endpoints: ["/client/files/analyze/cross-tab-analysis/predefined-colors"] },
      title: "Get Cross Tab Analysis Panel Predefined Colors",
      description: "List the predefined color palette available to cross-tab analysis panels (catalog — no ID), used to style pivot cells. Specific to cross-tab panels. Read-only.",
      inputSchema: {
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/files/analyze/cross-tab-analysis/predefined-colors";
        const query: Record<string, string> = {};
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_cross_tab_analysis_panel_predefined_colors", error);
      }
    },
  );
  registerTool(
    "get_dashboard",
    {
      _meta: { endpoints: ["/client/files/dashboard"] },
      title: "Get Dashboard",
      description: "Get one dashboard by ID — a saved arrangement of analysis widgets. Individual tiles are fetched with get_widget. Read-only.",
      inputSchema: {
      "ID": z.string().optional().describe("ID (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ ID, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/files/dashboard";
        const query: Record<string, string> = {};
      if (ID !== undefined) query["ID"] = String(ID);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_dashboard", error);
      }
    },
  );
  registerTool(
    "get_database_decision_rule",
    {
      _meta: { endpoints: ["/client/files/database-decision-rule"] },
      title: "Get Database Decision Rule",
      description: "Get one database decision rule by ID. A decision rule evaluates records against criteria to select an outcome; the DATABASE variant sources its criteria from a client database table. Sibling rules keyed by data source: json (get_json_decision_rule), web (get_web_decision_rule), orchestration (get_orchestration_decision_rule), attribute-list (get_attribute_list_decision_rule). Read-only.",
      inputSchema: {
      "ID": z.string().optional().describe("ID (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ ID, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/files/database-decision-rule";
        const query: Record<string, string> = {};
      if (ID !== undefined) query["ID"] = String(ID);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_database_decision_rule", error);
      }
    },
  );
  registerTool(
    "get_decision_rule_available_criterion",
    {
      _meta: { endpoints: ["/client/files/decision-rule/available-criterion"] },
      title: "Get Decision Rule Available Criterion",
      description: "Get the criteria a given decision rule can be built on — the queryable attributes/fields available to decision rule ID. Use when composing or inspecting rule logic. The SQL-database-definition equivalent is get_decision_rule_available_sql_database_criterion. Read-only.",
      inputSchema: {
      "ID": z.string().optional().describe("ID (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ ID, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/files/decision-rule/available-criterion";
        const query: Record<string, string> = {};
      if (ID !== undefined) query["ID"] = String(ID);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_decision_rule_available_criterion", error);
      }
    },
  );
  registerTool(
    "get_decision_rule_available_sql_database_criterion",
    {
      _meta: { endpoints: ["/client/files/decision-rule/available-sqldatabase-definition-criterion"] },
      title: "Get Decision Rule Available Sql Database Criterion",
      description: "Get the criteria available to a decision rule sourced from a SQL database definition (ID = the SQL database definition). Companion to get_decision_rule_available_criterion, which is the generic (non-SQL-definition) form. Read-only.",
      inputSchema: {
      "ID": z.string().optional().describe("ID (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ ID, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/files/decision-rule/available-sqldatabase-definition-criterion";
        const query: Record<string, string> = {};
      if (ID !== undefined) query["ID"] = String(ID);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_decision_rule_available_sql_database_criterion", error);
      }
    },
  );
  registerTool(
    "get_digital_html_content_asset",
    {
      _meta: { endpoints: ["/client/files/digital-assets/html-content"] },
      title: "Get Digital Html Content Asset",
      description: "Get one digital HTML content asset by ID — a reusable HTML content block for digital/email channels. Sibling asset types: text (get_digital_text_content_asset), image file (get_digital_image_content_file_asset). Read-only.",
      inputSchema: {
      "ID": z.string().optional().describe("ID (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ ID, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/files/digital-assets/html-content";
        const query: Record<string, string> = {};
      if (ID !== undefined) query["ID"] = String(ID);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_digital_html_content_asset", error);
      }
    },
  );
  registerTool(
    "get_digital_image_content_file_asset",
    {
      _meta: { endpoints: ["/client/files/digital-assets/image-content-file"] },
      title: "Get Digital Image Content File Asset",
      description: "Get one digital IMAGE content-file asset by ID. To browse all image content-file assets use list_digital_image_content_file_assets. Sibling types: HTML, text content assets. Read-only.",
      inputSchema: {
      "ID": z.string().optional().describe("ID (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ ID, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/files/digital-assets/image-content-file";
        const query: Record<string, string> = {};
      if (ID !== undefined) query["ID"] = String(ID);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_digital_image_content_file_asset", error);
      }
    },
  );
  registerTool(
    "list_digital_image_content_file_assets",
    {
      _meta: { endpoints: ["/client/files/digital-assets/image-content-files"] },
      title: "List Digital Image Content File Assets",
      description: "List (paged, via PageNumber/PageSize) the digital image content-file assets — the image library for digital channels. Fetch one by ID with get_digital_image_content_file_asset. Read-only.",
      inputSchema: {
      "PageNumber": z.string().optional().describe("PageNumber (query parameter)"),
      "PageSize": z.string().optional().describe("PageSize (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ PageNumber, PageSize, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/files/digital-assets/image-content-files";
        const query: Record<string, string> = {};
      if (PageNumber !== undefined) query["PageNumber"] = String(PageNumber);
      if (PageSize !== undefined) query["PageSize"] = String(PageSize);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in list_digital_image_content_file_assets", error);
      }
    },
  );
  registerTool(
    "get_digital_text_content_asset",
    {
      _meta: { endpoints: ["/client/files/digital-assets/text-content"] },
      title: "Get Digital Text Content Asset",
      description: "Get one digital TEXT content asset by ID — a reusable plain-text content block. Siblings: HTML (get_digital_html_content_asset), image file (get_digital_image_content_file_asset). Read-only.",
      inputSchema: {
      "ID": z.string().optional().describe("ID (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ ID, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/files/digital-assets/text-content";
        const query: Record<string, string> = {};
      if (ID !== undefined) query["ID"] = String(ID);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_digital_text_content_asset", error);
      }
    },
  );
  registerTool(
    "get_export_template",
    {
      _meta: { endpoints: ["/client/files/export-template"] },
      title: "Get Export Template",
      description: "Get one export template by ID — the reusable definition of columns and format for exporting audience or selection output. Read-only.",
      inputSchema: {
      "ID": z.string().optional().describe("ID (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ ID, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/files/export-template";
        const query: Record<string, string> = {};
      if (ID !== undefined) query["ID"] = String(ID);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_export_template", error);
      }
    },
  );
  registerTool(
    "get_googleads_customer_match_offer",
    {
      _meta: { endpoints: ["/client/files/offer/googleads"] },
      title: "Get Googleads Customer Match Offer",
      description: "Get one Google Ads Customer Match template offer by ID — the offer type that syncs an audience to a Google Ads Customer Match list. For a standard offer use get_offer. Read-only.",
      inputSchema: {
      "ID": z.string().optional().describe("ID (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ ID, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/files/offer/googleads";
        const query: Record<string, string> = {};
      if (ID !== undefined) query["ID"] = String(ID);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_googleads_customer_match_offer", error);
      }
    },
  );
  registerTool(
    "get_data_process_default_parameters",
    {
      _meta: { endpoints: ["/client/files/interaction/data-process-default-parameters"] },
      title: "Get Data Process Default Parameters",
      description: "Get the default parameter values for a data-process activity inside an interaction workflow — keyed by InteractionID + WorkflowAssociationID + DataProcessActivityID. Use before configuring or running that activity. Read-only.",
      inputSchema: {
      "InteractionID": z.string().optional().describe("InteractionID (query parameter)"),
      "WorkflowAssociationID": z.string().optional().describe("WorkflowAssociationID (query parameter)"),
      "DataProcessActivityID": z.string().optional().describe("DataProcessActivityID (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ InteractionID, WorkflowAssociationID, DataProcessActivityID, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/files/interaction/data-process-default-parameters";
        const query: Record<string, string> = {};
      if (InteractionID !== undefined) query["InteractionID"] = String(InteractionID);
      if (WorkflowAssociationID !== undefined) query["WorkflowAssociationID"] = String(WorkflowAssociationID);
      if (DataProcessActivityID !== undefined) query["DataProcessActivityID"] = String(DataProcessActivityID);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_data_process_default_parameters", error);
      }
    },
  );
  registerTool(
    "get_decision_offer_test_channel_fulfillment_states",
    {
      _meta: { endpoints: ["/client/files/interaction/decision-offer/test-channel-fulfillment-states"] },
      title: "Get Decision Offer Test Channel Fulfillment States",
      description: "Get the test channel fulfillment states for a decision-offer interaction (ID = the decision-offer file) — the simulated per-channel delivery states used when testing an offer decision. Read-only.",
      inputSchema: {
      "ID": z.string().optional().describe("ID (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ ID, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/files/interaction/decision-offer/test-channel-fulfillment-states";
        const query: Record<string, string> = {};
      if (ID !== undefined) query["ID"] = String(ID);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_decision_offer_test_channel_fulfillment_states", error);
      }
    },
  );
  registerTool(
    "get_json_decision_rule",
    {
      _meta: { endpoints: ["/client/files/json-decision-rule"] },
      title: "Get Json Decision Rule",
      description: "Get one JSON decision rule by ID — a decision rule whose input is a JSON message/payload (real-time / inbound decisioning) rather than a stored table. Siblings by source: database (get_database_decision_rule), web (get_web_decision_rule), orchestration (get_orchestration_decision_rule), attribute-list (get_attribute_list_decision_rule). Read-only.",
      inputSchema: {
      "ID": z.string().optional().describe("ID (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ ID, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/files/json-decision-rule";
        const query: Record<string, string> = {};
      if (ID !== undefined) query["ID"] = String(ID);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_json_decision_rule", error);
      }
    },
  );
  registerTool(
    "get_model_project",
    {
      _meta: { endpoints: ["/client/files/model-project"] },
      title: "Get Model Project",
      description: "Get one predictive model project by ID — the container for a data-science model's configuration and outputs. Read-only.",
      inputSchema: {
      "ID": z.string().optional().describe("ID (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ ID, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/files/model-project";
        const query: Record<string, string> = {};
      if (ID !== undefined) query["ID"] = String(ID);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_model_project", error);
      }
    },
  );
  registerTool(
    "get_offer",
    {
      _meta: { endpoints: ["/client/files/offer"] },
      title: "Get Offer",
      description: "Get one offer by ID — a marketing offer definition served through interactions/decisions. The Google Ads Customer Match template variant has its own reader: get_googleads_customer_match_offer. Read-only.",
      inputSchema: {
      "ID": z.string().optional().describe("ID (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ ID, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/files/offer";
        const query: Record<string, string> = {};
      if (ID !== undefined) query["ID"] = String(ID);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_offer", error);
      }
    },
  );
  registerTool(
    "get_orchestration_decision_rule",
    {
      _meta: { endpoints: ["/client/files/orchestration-decision-rule"] },
      title: "Get Orchestration Decision Rule",
      description: "Get one orchestration decision rule by ID — the decision rule type used inside interaction/orchestration workflows to branch a customer journey. Its referenced dynamic-content files are paged by list_orchestration_decision_rule_dynamic_content_files. Siblings: database, json, web, attribute-list decision rules. Read-only.",
      inputSchema: {
      "ID": z.string().optional().describe("ID (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ ID, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/files/orchestration-decision-rule";
        const query: Record<string, string> = {};
      if (ID !== undefined) query["ID"] = String(ID);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_orchestration_decision_rule", error);
      }
    },
  );
  registerTool(
    "list_orchestration_decision_rule_dynamic_content_files",
    {
      _meta: { endpoints: ["/client/files/orchestration-decision-rule/dynamic-content-files"] },
      title: "List Orchestration Decision Rule Dynamic Content Files",
      description: "List (paged, via PageNumber/PageSize) the dynamic-content files referenced by orchestration decision rules — the content assets such a rule can serve. For the rule itself use get_orchestration_decision_rule. Read-only.",
      inputSchema: {
      "PageNumber": z.string().optional().describe("PageNumber (query parameter)"),
      "PageSize": z.string().optional().describe("PageSize (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ PageNumber, PageSize, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/files/orchestration-decision-rule/dynamic-content-files";
        const query: Record<string, string> = {};
      if (PageNumber !== undefined) query["PageNumber"] = String(PageNumber);
      if (PageSize !== undefined) query["PageSize"] = String(PageSize);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in list_orchestration_decision_rule_dynamic_content_files", error);
      }
    },
  );
  registerTool(
    "get_channel_overview_results",
    {
      _meta: { endpoints: ["/client/files/reports/channel-overview-results"] },
      title: "Get Channel Overview Results",
      description: "Get channel overview report results for a channel over a date range — ChannelID plus StartDate/EndDate, optionally filtered by interaction, offer, or workflow association. Returns delivery/response metrics for the channel. Read-only.",
      inputSchema: {
      "ChannelID": z.string().optional().describe("ChannelID (query parameter)"),
      "StartDate": z.string().optional().describe("StartDate (query parameter)"),
      "EndDate": z.string().optional().describe("EndDate (query parameter)"),
      "FilterOnInteractionID": z.string().optional().describe("FilterOnInteractionID (query parameter)"),
      "FilterOnOfferID": z.string().optional().describe("FilterOnOfferID (query parameter)"),
      "FilterOnWorkflowAssociationID": z.string().optional().describe("FilterOnWorkflowAssociationID (query parameter)"),
      "FilterResultsOnSpecificChannel": z.string().optional().describe("FilterResultsOnSpecificChannel (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ ChannelID, StartDate, EndDate, FilterOnInteractionID, FilterOnOfferID, FilterOnWorkflowAssociationID, FilterResultsOnSpecificChannel, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/files/reports/channel-overview-results";
        const query: Record<string, string> = {};
      if (ChannelID !== undefined) query["ChannelID"] = String(ChannelID);
      if (StartDate !== undefined) query["StartDate"] = String(StartDate);
      if (EndDate !== undefined) query["EndDate"] = String(EndDate);
      if (FilterOnInteractionID !== undefined) query["FilterOnInteractionID"] = String(FilterOnInteractionID);
      if (FilterOnOfferID !== undefined) query["FilterOnOfferID"] = String(FilterOnOfferID);
      if (FilterOnWorkflowAssociationID !== undefined) query["FilterOnWorkflowAssociationID"] = String(FilterOnWorkflowAssociationID);
      if (FilterResultsOnSpecificChannel !== undefined) query["FilterResultsOnSpecificChannel"] = String(FilterResultsOnSpecificChannel);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_channel_overview_results", error);
      }
    },
  );
  registerTool(
    "list_standard_selection_rule_entire_rule_types",
    {
      _meta: { endpoints: ["/client/files/standard-selection-rule/advance-entire-rule/types-and-id"] },
      title: "List Standard Selection Rule Entire Rule Types",
      description: "List the advance 'entire rule' types and their IDs available when building a standard selection rule (catalog — no ID). For a specific standard selection rule use the get_standard_selection_rule_by_id tool. Read-only.",
      inputSchema: {
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/files/standard-selection-rule/advance-entire-rule/types-and-id";
        const query: Record<string, string> = {};
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in list_standard_selection_rule_entire_rule_types", error);
      }
    },
  );
  registerTool(
    "get_venn_analysis_panel",
    {
      _meta: { endpoints: ["/client/files/analyze/venn-analysis"] },
      title: "Get Venn Analysis Panel",
      description: "Get one venn analysis panel by ID — an overlap/set analysis across audiences or segments. Siblings: chart (get_chart_analysis_panel), cross-tab (get_cross_tab_analysis_panel). Read-only.",
      inputSchema: {
      "ID": z.string().optional().describe("ID (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ ID, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/files/analyze/venn-analysis";
        const query: Record<string, string> = {};
      if (ID !== undefined) query["ID"] = String(ID);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_venn_analysis_panel", error);
      }
    },
  );
  registerTool(
    "get_web_decision_rule",
    {
      _meta: { endpoints: ["/client/files/web-decision-rule"] },
      title: "Get Web Decision Rule",
      description: "Get one web decision rule by ID — a decision rule that fires on a website / web-channel interaction. Its available site connectors are listed by list_web_decision_rule_adaptors. Siblings: database, json, orchestration, attribute-list decision rules. Read-only.",
      inputSchema: {
      "ID": z.string().optional().describe("ID (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ ID, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/files/web-decision-rule";
        const query: Record<string, string> = {};
      if (ID !== undefined) query["ID"] = String(ID);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_web_decision_rule", error);
      }
    },
  );
  registerTool(
    "list_web_decision_rule_adaptors",
    {
      _meta: { endpoints: ["/client/files/web-decision-rule/web-adaptors"] },
      title: "List Web Decision Rule Adaptors",
      description: "List the web adaptors (site/channel connectors) available to web decision rules. Catalog lookup — no ID. For a specific rule use get_web_decision_rule. Read-only.",
      inputSchema: {
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/files/web-decision-rule/web-adaptors";
        const query: Record<string, string> = {};
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in list_web_decision_rule_adaptors", error);
      }
    },
  );
  registerTool(
    "get_widget",
    {
      _meta: { endpoints: ["/client/files/widget"] },
      title: "Get Widget",
      description: "Get one dashboard widget by ID — a single visualization tile (chart/metric) that lives on a dashboard (get_dashboard). Read-only.",
      inputSchema: {
      "ID": z.string().optional().describe("ID (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ ID, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/files/widget";
        const query: Record<string, string> = {};
      if (ID !== undefined) query["ID"] = String(ID);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_widget", error);
      }
    },
  );
}
