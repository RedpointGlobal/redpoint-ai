// AUTO-GENERATED by scripts/generate-tools.ts — DO NOT EDIT.
// In-scope RPI GET endpoints (#27634). Regenerate: bun run generate:tools
// All tools are read-only (GET); descriptions come from src/tools/overlay.ts.

import { z } from "zod";
import type { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js";
import type { RPIApiClient } from "../../client/rpi-api.js";
import { createToolRegistrar } from "../../tool-categories.js";
import { clientIdSchema, verboseSchema, jsonContent, errorContent, READ_ONLY_ANNOTATIONS, targetUrlOf } from "../generated-shared.js";

export function registerWorkflowsGeneratedTools(server: McpServer, rpiClient: RPIApiClient): void {
  const registerTool = createToolRegistrar(server, "workflows");
  void rpiClient;
  registerTool(
    "get_workflow_activity_results",
    {
      _meta: { endpoints: ["/client/workflows/instances/activity-results"] },
      title: "Get Workflow Activity Results",
      description: "Get the RESULTS/output of a single activity (ActivityID) within a workflow run (WorkflowAssociationInstanceID) — the data that activity produced. Same activity, other artifacts: log (get_workflow_activity_log), assets (get_workflow_activity_assets), SQL trace (get_workflow_activity_sql_trace). For results across ALL activities of the run: get_workflow_all_activity_results. Read-only.",
      inputSchema: {
      "WorkflowAssociationInstanceID": z.string().optional().describe("WorkflowAssociationInstanceID (query parameter)"),
      "ActivityID": z.string().optional().describe("ActivityID (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ WorkflowAssociationInstanceID, ActivityID, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/workflows/instances/activity-results";
        const query: Record<string, string> = {};
      if (WorkflowAssociationInstanceID !== undefined) query["WorkflowAssociationInstanceID"] = String(WorkflowAssociationInstanceID);
      if (ActivityID !== undefined) query["ActivityID"] = String(ActivityID);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_workflow_activity_results", error);
      }
    },
  );
  registerTool(
    "get_workflow_activity_assets",
    {
      _meta: { endpoints: ["/client/workflows/instances/activity-assets"] },
      title: "Get Workflow Activity Assets",
      description: "Get the ASSETS produced by a single activity (ActivityID) within a workflow run (WorkflowAssociationInstanceID) — the files/outputs it generated. Same activity, other artifacts: results / log / SQL trace. Read-only.",
      inputSchema: {
      "WorkflowAssociationInstanceID": z.string().optional().describe("WorkflowAssociationInstanceID (query parameter)"),
      "ActivityID": z.string().optional().describe("ActivityID (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ WorkflowAssociationInstanceID, ActivityID, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/workflows/instances/activity-assets";
        const query: Record<string, string> = {};
      if (WorkflowAssociationInstanceID !== undefined) query["WorkflowAssociationInstanceID"] = String(WorkflowAssociationInstanceID);
      if (ActivityID !== undefined) query["ActivityID"] = String(ActivityID);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_workflow_activity_assets", error);
      }
    },
  );
  registerTool(
    "list_workflow_instance_summaries",
    {
      _meta: { endpoints: ["/client/workflows/instances/summaries"] },
      title: "List Workflow Instance Summaries",
      description: "List the per-activity summaries for a workflow run (WorkflowAssociationInstanceID / ActivityID) — status/counts per activity. Distinct from the get_workflow_instance_summary hand tool (the single run-level summary). Read-only.",
      inputSchema: {
      "ActivityID": z.string().optional().describe("ActivityID (query parameter)"),
      "WorkflowAssociationInstanceID": z.string().optional().describe("WorkflowAssociationInstanceID (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ ActivityID, WorkflowAssociationInstanceID, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/workflows/instances/summaries";
        const query: Record<string, string> = {};
      if (ActivityID !== undefined) query["ActivityID"] = String(ActivityID);
      if (WorkflowAssociationInstanceID !== undefined) query["WorkflowAssociationInstanceID"] = String(WorkflowAssociationInstanceID);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in list_workflow_instance_summaries", error);
      }
    },
  );
  registerTool(
    "get_workflow_activity_log",
    {
      _meta: { endpoints: ["/client/workflows/instances/activity-log"] },
      title: "Get Workflow Activity Log",
      description: "Get the execution LOG of a single activity (ActivityID) within a workflow run (WorkflowAssociationInstanceID) — its run messages/timeline. Same activity, other artifacts: results / assets / SQL trace. For the whole run's log use get_workflow_association_instance_logs. Read-only.",
      inputSchema: {
      "WorkflowAssociationInstanceID": z.string().optional().describe("WorkflowAssociationInstanceID (query parameter)"),
      "ActivityID": z.string().optional().describe("ActivityID (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ WorkflowAssociationInstanceID, ActivityID, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/workflows/instances/activity-log";
        const query: Record<string, string> = {};
      if (WorkflowAssociationInstanceID !== undefined) query["WorkflowAssociationInstanceID"] = String(WorkflowAssociationInstanceID);
      if (ActivityID !== undefined) query["ActivityID"] = String(ActivityID);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_workflow_activity_log", error);
      }
    },
  );
  registerTool(
    "get_workflow_audience_block_instance_results",
    {
      _meta: { endpoints: ["/client/workflows/audiences/block-instance-results"] },
      title: "Get Workflow Audience Block Instance Results",
      description: "Get the results of ONE audience block instance (BlockInstanceID) within an audience-workflow activity (ActivityID + WorkflowAssociationInstanceID) — that single block's membership/counts. For ALL blocks of the activity use the get_audience_workflow_block_results hand tool (plural). Read-only.",
      inputSchema: {
      "ActivityID": z.string().optional().describe("ActivityID (query parameter)"),
      "WorkflowAssociationInstanceID": z.string().optional().describe("WorkflowAssociationInstanceID (query parameter)"),
      "BlockInstanceID": z.string().optional().describe("BlockInstanceID (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ ActivityID, WorkflowAssociationInstanceID, BlockInstanceID, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/workflows/audiences/block-instance-results";
        const query: Record<string, string> = {};
      if (ActivityID !== undefined) query["ActivityID"] = String(ActivityID);
      if (WorkflowAssociationInstanceID !== undefined) query["WorkflowAssociationInstanceID"] = String(WorkflowAssociationInstanceID);
      if (BlockInstanceID !== undefined) query["BlockInstanceID"] = String(BlockInstanceID);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_workflow_audience_block_instance_results", error);
      }
    },
  );
  registerTool(
    "get_workflow_activity_sql_trace",
    {
      _meta: { endpoints: ["/client/workflows/instances/activity-sql-trace"] },
      title: "Get Workflow Activity Sql Trace",
      description: "Get the SQL TRACE of a single activity (ActivityID) within a workflow run (WorkflowAssociationInstanceID) — the SQL it executed, for debugging. Same activity, other artifacts: results / log / assets. Read-only.",
      inputSchema: {
      "WorkflowAssociationInstanceID": z.string().optional().describe("WorkflowAssociationInstanceID (query parameter)"),
      "ActivityID": z.string().optional().describe("ActivityID (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ WorkflowAssociationInstanceID, ActivityID, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/workflows/instances/activity-sql-trace";
        const query: Record<string, string> = {};
      if (WorkflowAssociationInstanceID !== undefined) query["WorkflowAssociationInstanceID"] = String(WorkflowAssociationInstanceID);
      if (ActivityID !== undefined) query["ActivityID"] = String(ActivityID);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_workflow_activity_sql_trace", error);
      }
    },
  );
  registerTool(
    "get_workflow_instance_logs",
    {
      _meta: { endpoints: ["/client/workflows/logs/workflow-instance"] },
      title: "Get Workflow Instance Logs",
      description: "Get the logs for a workflow instance by its ID — the workflow-instance-scoped log. Related: get_workflow_association_instance_logs (the association-instance run log) and get_workflow_activity_log (a single activity's log). Read-only.",
      inputSchema: {
      "ID": z.string().optional().describe("ID (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ ID, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/workflows/logs/workflow-instance";
        const query: Record<string, string> = {};
      if (ID !== undefined) query["ID"] = String(ID);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_workflow_instance_logs", error);
      }
    },
  );
  registerTool(
    "get_workflow_association_instance_logs",
    {
      _meta: { endpoints: ["/client/workflows/logs/workflow-association-instance"] },
      title: "Get Workflow Association Instance Logs",
      description: "Get the logs for a whole workflow-association instance (WorkflowAssociationInstanceID) — the run-level log across the workflow. For a single activity's log use get_workflow_activity_log; for the workflow-instance-scoped log use get_workflow_instance_logs. Read-only.",
      inputSchema: {
      "WorkflowAssociationInstanceID": z.string().optional().describe("WorkflowAssociationInstanceID (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ WorkflowAssociationInstanceID, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/workflows/logs/workflow-association-instance";
        const query: Record<string, string> = {};
      if (WorkflowAssociationInstanceID !== undefined) query["WorkflowAssociationInstanceID"] = String(WorkflowAssociationInstanceID);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_workflow_association_instance_logs", error);
      }
    },
  );
  registerTool(
    "get_workflow_all_activity_results",
    {
      _meta: { endpoints: ["/client/workflows/instances/all-activity-results"] },
      title: "Get Workflow All Activity Results",
      description: "Get the results of ALL activities in a workflow run (WorkflowAssociationInstanceID) — every activity's output in one call. For a single activity use get_workflow_activity_results. Read-only.",
      inputSchema: {
      "WorkflowAssociationInstanceID": z.string().optional().describe("WorkflowAssociationInstanceID (query parameter)"),
      clientId: clientIdSchema,
      verbose: verboseSchema,
      },
      annotations: READ_ONLY_ANNOTATIONS,
    },
    async ({ WorkflowAssociationInstanceID, clientId, verbose }, extra) => {
      const userToken = extra.authInfo?.token;
      try {
        let path = "/client/workflows/instances/all-activity-results";
        const query: Record<string, string> = {};
      if (WorkflowAssociationInstanceID !== undefined) query["WorkflowAssociationInstanceID"] = String(WorkflowAssociationInstanceID);
        const result = await rpiClient.get(userToken, path, query, { clientId, verbose, baseUrl: targetUrlOf(extra) });
        return jsonContent(result);
      } catch (error) {
        return errorContent("Error in get_workflow_all_activity_results", error);
      }
    },
  );
}
